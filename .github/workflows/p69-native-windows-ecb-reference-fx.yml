name: P69 Native Windows ECB Reference FX Legacy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by p69-native-windows-ecb-reference-fx-v2.yml"
