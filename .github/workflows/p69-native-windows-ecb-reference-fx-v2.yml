name: P69R1 Native Windows ECB Reference FX Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by P69R2 after TypeScript semantic failure of the first Node24 egress repair."
