name: P69 Native Windows ECB Reference FX V2 Paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while P69R1 Node24 pinned-egress repair is prepared."
