{
  "installCommand": "npm_config_engine_strict=false npm ci --no-audit --no-fund --progress=false",
  "buildCommand": "next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/market-integrity/cron?pages=2&perPage=100",
      "schedule": "*/30 * * * *"
    }
  ]
}
