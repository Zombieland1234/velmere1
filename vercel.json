{
  "installCommand": "npm ci --engine-strict=true --strict-allow-scripts=true --no-audit --no-fund --progress=false",
  "buildCommand": "npm run build:deployment",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/internal/workers/verify-continuous-monitor",
      "schedule": "0 3 * * *"
    }
  ]
}
