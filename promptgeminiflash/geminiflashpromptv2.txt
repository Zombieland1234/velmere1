# VELMÈRE — ULTIMATE END-TO-END FINALIZATION MISSION

## FULL CODEBASE • FULL DATA • FULL PROVIDERS • FULL SECURITY • FULL AI • FULL CUSTOMERS • FULL TIERS • FULL PAYMENTS • FULL COMMERCIAL READINESS

## FINAL BUILD-TO-SALE GATE — DO NOT STOP UNTIL THE REAL PRODUCT IS COMPLETE

---

# 0. THIS OVERRIDES PREVIOUS REPORTS

Treat all previous:

* PASS reports,
* COMPLETE statuses,
* SEALED statuses,
* release gates,
* customer simulations,
* auditor results,

as **historical evidence only**.

They are NOT automatically trusted.

You must independently verify them against the current repository and current runtime.

Your mission is NOT:

> "prove previous reports were correct."

Your mission is:

# "MAKE VELMÈRE ACTUALLY COMPLETE."

If an old report says COMPLETE but the current code is broken:

## FIX IT.

If an old report says PASS but the feature is only simulated:

## FIX IT / REPLACE IT / EXPLICITLY CLASSIFY IT.

If a feature can be implemented:

# IMPLEMENT IT.

If a feature requires a genuine external dependency:

# IMPLEMENT EVERYTHING POSSIBLE AND RECORD ONLY THE REAL EXTERNAL BLOCKER.

Never fabricate the missing part.

---

# 1. FINAL OBJECTIVE

Bring the project to:

```text
CODE COMPLETE
DATA COMPLETE
PROVIDER COMPLETE
RIGHTS-AWARE
SECURE
STABLE
TESTED
CUSTOMER-USABLE
TIER-CORRECT
PAYMENT-CORRECT
PDF-CORRECT
AI-CORRECT
MOBILE-CORRECT
PERFORMANCE-CORRECT
COMMERCIAL-READY
```

The final system must work as one chain:

```text
CUSTOMER
  ↓
AUTH
  ↓
PRODUCT
  ↓
DATA
  ↓
PROVIDER
  ↓
NORMALIZATION
  ↓
PROVENANCE
  ↓
RIGHTS
  ↓
RISK / ANALYSIS
  ↓
EVIDENCE
  ↓
TIER
  ↓
OUTPUT
  ↓
PDF / REPORT
  ↓
PAYMENT / ENTITLEMENT
  ↓
CUSTOMER
```

---

# 2. NON-NEGOTIABLE RULE

When you find a problem:

```text
DISCOVER
↓
REPRODUCE
↓
ROOT CAUSE
↓
IMPLEMENT FIX
↓
TEST
↓
LIVE VERIFY
↓
REGRESSION
```

DO NOT stop at:

`FAIL`

when the issue can be fixed.

DO NOT stop at:

`NOT_IMPLEMENTED`

when implementation is technically possible.

DO NOT lower the test standard just to make it pass.

---

# 3. FULL REPOSITORY FORENSIC SCAN

Search the entire repository.

Search for:

```text
TODO
FIXME
HACK
TEMP
MOCK
MOCKED
STUB
PLACEHOLDER
FAKE
SIMULATION
SIMULATED
DEMO
REFERENCE
REFERENCE_ONLY
LOCAL_ONLY
DEV_ONLY
SEED
FIXTURE
HARDCODED
HARDCODE
UNKNOWN
UNVERIFIED
WITHHELD
BLOCKED
NOT_IMPLEMENTED
NOT_FOR_SALE
MISSING_PROOF
FALLBACK
LOCAL_DOUBLE
BYPASS
DISABLE
SKIP
IGNORE
@ts-ignore
eslint-disable
catch {}
swallow
silent
```

Also search for:

* hardcoded market prices,
* hardcoded timestamps,
* fake scores,
* fake customer IDs,
* fake entitlement IDs,
* fake payment states,
* static provider responses,
* static screenshots,
* demo unlocks,
* hidden test bypasses.

Classify every hit:

```text
TEST-ONLY
DEV-ONLY
VALID FALLBACK
PRODUCTION
BUG
```

Any accidental production mock:

# REMOVE / REPLACE / FIX.

---

# 4. CODE QUALITY + INTERNAL CONSISTENCY

Audit:

* imports,
* exports,
* duplicate functions,
* duplicate types,
* dead code,
* circular dependencies,
* unreachable branches,
* contradictory feature flags,
* inconsistent enums,
* inconsistent tier names,
* inconsistent status values,
* inconsistent API shapes,
* nullable values,
* unsafe casts,
* error swallowing,
* race conditions,
* state synchronization.

Check:

```text
TypeScript
ESLint
Build
Next.js
React
Server Components
Client Components
API routes
Middleware
Database layer
```

Run:

```text
tsc --noEmit
lint
production build
```

Fix all actual errors.

Warnings that represent real reliability problems must also be fixed.

---

# 5. ENVIRONMENT / SECRETS AUDIT

Inspect:

* `.env`
* `.env.local`
* `.env.example`
* `.gitignore`
* CI
* deployment configuration
* client bundles
* server logs.

Verify:

* secrets server-only,
* public values explicitly PUBLIC,
* API keys never sent to client,
* HMAC secrets strong,
* signing keys separated by purpose,
* rotation-ready secrets,
* no secrets in screenshots,
* no secrets in PDFs,
* no secrets in logs,
* no secrets in AI prompts unnecessarily.

Never commit or expose real credentials.

If `.env.local` contains production secrets:

# DO NOT COPY THEM INTO SOURCE CONTROL.

---

# 6. CLEAN SERVER BOOT

Before final acceptance:

1. Kill all old Node/Next processes.
2. Verify the port is free.
3. Start a fresh server.
4. Record PID.
5. Confirm fresh boot.
6. Confirm current environment.
7. Confirm no crash loop.
8. Confirm no hydration errors.
9. Confirm no critical console errors.

Then perform ALL testing against this fresh process.

---

# 7. PROVIDER MASTER INVENTORY

Discover every provider referenced anywhere in:

* code,
* docs,
* knowledge base,
* env,
* tests,
* configuration.

Build:

```text
FINAL_PROVIDER_MATRIX.md
```

with:

| Provider | Endpoint | Data | Auth | Live | Free | Commercial | Derived Use | Raw Redistribution | UI | PDF | Attribution | Rate Limit | Rights | Status |
| -------- | -------- | ---- | ---- | ---- | ---- | ---------- | ----------- | ------------------ | -- | --- | ----------- | ---------- | ------ | ------ |

---

# 8. CURRENT INTERNET VERIFICATION

For every important provider, use CURRENT official sources.

Verify:

* documentation,
* terms,
* licensing,
* pricing,
* free tier,
* commercial usage,
* API limits,
* redistribution,
* attribution,
* data freshness,
* caching,
* retention,
* customer-facing use.

Record:

```text
URL
date checked
claim verified
interpretation
confidence
```

No current source = no strong claim.

---

# 9. COINGECKO SPECIFIC CHECK

Do NOT assume:

```text
free API = commercial rights
```

Current official CoinGecko documentation distinguishes the free Demo access from commercial licensing.

Standard commercial plans allow users to monetize products incorporating CoinGecko data, but the API itself cannot simply be resold/redistributed/syndicated, and required attribution must be displayed.

Therefore audit:

```text
CoinGecko Demo
CoinGecko commercial plan
CoinGecko Enterprise/custom licensing
```

and determine which exact state VELMÈRE is actually in.

If VELMÈRE is using CoinGecko in a commercial product without the required license:

# DO NOT FAKE SALES READINESS.

Either:

* switch to a legitimately usable provider path,
* use CoinGecko only within a permitted use case,
* obtain the appropriate plan,
* or keep the relevant customer delivery withheld.

Source of current verification:
CoinGecko pricing/licensing documentation.

---

# 10. BINANCE / MARKET DATA

Verify current official API/terms.

Do not infer:

```text
public endpoint = unlimited commercial redistribution
```

Separate:

```text
data ingestion
internal analysis
derived metrics
customer display
raw redistribution
```

If Velmère is displaying derived risk analytics:

prove that exact flow.

If raw data cannot be redistributed:

do not expose the raw feed as the commercial product.

---

# 11. SEC EDGAR

Verify current SEC developer rules.

Respect Fair Access.

Implement:

* proper User-Agent,
* request throttling,
* caching,
* backoff,
* bounded retries,
* no aggressive crawling.

SEC currently states a total limit of no more than 10 requests/second per user, and asks developers to use efficient scripting and moderate requests.

---

# 12. CFTC

Verify source-by-source.

Government information on the CFTC site is generally public domain, with requested acknowledgement, but materials contributed or licensed by private parties can have separate rights.

Do not classify every CFTC asset automatically as unrestricted.

---

# 13. WORLD BANK / ECB

For World Bank:

verify dataset license individually.

Do not assume:

```text
World Bank API = universally unrestricted commercial reuse
```

For ECB:

verify whether the actual dataset is public ECB/ESCB statistical material or third-party content.

Rights must be attached to the actual source, not the domain name.

---

# 14. AI PROVIDER AUDIT

Inspect:

* Gemini
* Groq
* OpenRouter
* Ollama
* every fallback model.

Check current model availability.

Google currently lists Gemini 3.8 Flash as GA and production-ready, including a 1M token context window. Verify whether this is the correct primary model for the actual VELMÈRE deployment.

Do not leave stale model IDs without testing them.

---

# 15. AI DATA PRIVACY

Distinguish:

```text
PUBLIC DATA
LOW-SENSITIVITY CUSTOMER DATA
PRIVATE CUSTOMER DATA
AUDIT DATA
SECRETS
```

Do not send sensitive customer audit material through a provider/tier that is unsuitable for that data policy.

Google's current pricing documentation distinguishes Free Tier from Paid Tier regarding whether content may be used to improve Google products. Audit this explicitly.

Implement routing if needed:

```text
PUBLIC → appropriate provider
PRIVATE → approved provider
SENSITIVE → approved private/local path
SECRET → NEVER SEND TO LLM
```

---

# 16. ANGEL FINAL AUDIT

Test:

* grounding,
* RAG,
* numerical claim binding,
* evidence,
* memory,
* feedback,
* session continuity,
* provider fallback,
* prompt injection,
* jailbreak,
* secret exfiltration,
* tenant isolation,
* investment advice,
* leverage,
* fabricated probability,
* stale data,
* contradictory data.

Test:

PL
EN
DE

Ensure:

```text
AI disclosure
advice abstention
grounding
evidence binding
```

remain intact.

---

# 17. RISK ENGINE FULL RE-VERIFICATION

Test:

* normal,
* zero,
* negative,
* extreme,
* null,
* stale,
* missing,
* contradictory,
* malformed.

Test determinism.

Run repeated identical inputs.

Verify:

```text
INPUT
→ NORMALIZE
→ FORMULA
→ SCORE
→ BAND
→ EVIDENCE
```

No unexplained constants.

No arbitrary scores.

No fabricated certainty.

---

# 18. SECURITY AUDIT PRODUCT

Test real contracts plus adversarial corpus.

Minimum:

* owner controls,
* pause,
* mint,
* blacklist,
* taxes,
* reentrancy,
* proxy,
* upgradeability,
* tx.origin,
* unchecked calls,
* unbounded loops,
* selfdestruct,
* multicall,
* suspicious external calls.

Basic:

real prescreen.

Pro:

real deeper analysis.

Advanced:

deepest supported analysis + genuine human-review workflow when required.

Never call static analysis a formal proof unless it really is one.

---

# 19. SHIELD FINAL TEST

Test minimum:

BTC
ETH
SOL
BNB
XRP
ADA
DOGE
AVAX
LINK
DOT
LTC
TRX
TON
SHIB
UNI
ATOM
NEAR
APT
ARB
OP
SUI
PEPE
USDT
USDC
POL

plus additional supported assets.

Verify for every asset:

* price,
* volume,
* market cap,
* changes,
* sparkline,
* timestamp,
* provider,
* evidence,
* risk,
* modal.

Detect:

```text
undefined
null
NaN
Infinity
future timestamps
expired timestamps
invalid prices
```

---

# 20. SHIELD PRO FINAL VALUE TEST

Verify:

* real orderbook,
* real depth,
* bid/ask,
* spread,
* $10k slippage,
* stress tests,
* liquidity pressure,
* whale analysis,
* evidence,
* timestamps,
* provider provenance.

Then compare same asset:

```text
SHIELD BASIC
vs
SHIELD PRO
```

The report must show exactly what Pro adds.

---

# 21. REAL MARKETS

Test:

### Stocks

AAPL
NVDA
MSFT
GOOGL
AMZN
META
TSLA
JPM
AMD

### ETF

SPY
QQQ
GLD
SLV
EEM
VNQ
IYR

### REIT

PLD

### FX

EUR/USD
GBP/USD
USD/JPY
EUR/PLN
USD/PLN

### Commodities

Gold
Silver
WTI
Brent
Copper

### Indices

S&P 500
NASDAQ
Dow
Euro Stoxx 50
Nikkei 225

For every asset:

```text
catalog
→ quote
→ timestamp
→ provider
→ normalization
→ provenance
→ table
→ chart
→ modal
→ fallback
```

---

# 22. BROWSER / LENS

Test:

BTC
ETH
AAPL
NVDA
SPY
EUR
unknown asset
invalid input
unsupported URL
evidence-poor page
contradictory page
malicious input

Test:

```text
search
→ evidence
→ source
→ attribution
→ report
→ PDF
```

---

# 23. PDF

Generate REAL PDFs.

Test:

* Basic,
* Pro,
* Advanced internal/test path.

Verify:

* correct data,
* correct customer,
* correct report ID,
* evidence,
* timestamps,
* source,
* hash,
* ownership,
* authorization,
* download security,
* preview/download consistency.

Check whether:

```text
preview
=
download
```

where intended.

---

# 24. BASIC / PRO / ADVANCED MASTER TIER AUDIT

Do NOT use one global Pro result.

Create separate decisions:

```text
Security Audit Basic
Security Audit Pro
Security Audit Advanced

Shield Basic
Shield Pro

Real Markets Basic
Real Markets Pro

Browser/Lens Basic
Browser/Lens Pro

Angel
```

For each:

* implemented,
* live,
* data-backed,
* evidence-backed,
* server-enforced,
* customer-visible,
* customer-useful,
* commercially deliverable,
* external dependencies.

---

# 25. CUSTOMER VALUE TEST

For every paid product/tier answer:

```text
WHAT DID THE CUSTOMER GET?
WHAT WAS UNIQUE?
WHAT WAS DEEPER THAN BASIC?
WHAT DATA SUPPORTED IT?
WHAT EVIDENCE SUPPORTED IT?
WHAT MADE IT WORTH PAYING FOR?
WHAT WAS STILL MISSING?
```

No generic:

`"Pro is better"`

allowed.

Give concrete differences.

---

# 26. AI AUDITOR PANEL — MINIMUM 30

Expand to at least 30 independent auditor roles.

Include:

* security,
* auth,
* RLS,
* provider,
* rights,
* data integrity,
* risk,
* audit,
* Shield,
* Shield Pro,
* Real Markets,
* Browser,
* Angel,
* PDF,
* Payments,
* Entitlements,
* Accessibility,
* UX,
* Performance,
* Privacy,
* Commercial,
* Customer Value,
* Pricing,
* Reliability,
* Failure recovery,
* adversarial testing,
* multilingual,
* independent skeptic,
* red-team,
* final auditor.

Auditors must be able to fail the product.

No forced positive output.

---

# 27. AI CUSTOMER PANEL — MINIMUM 200

Use:

### 50 Basic

### 100 Pro

### 50 Advanced / Institutional

Advanced can be an internal evaluation cohort when public purchase is not yet permitted.

Do not simulate a real payment as if it happened.

Mark:

`SIMULATED CUSTOMER EVIDENCE`

---

# 28. CUSTOMER BEHAVIOR

Customers must perform actions.

Not just answer prompts.

Examples:

```text
open
search
select
inspect
compare
analyze
view evidence
run audit
open table
open modal
generate PDF
download PDF
compare tier
try upgrade
use Angel
return
```

---

# 29. CUSTOMER TYPES

Include:

* beginner,
* expert,
* professional analyst,
* crypto trader,
* DeFi researcher,
* stock investor,
* ETF investor,
* FX analyst,
* macro analyst,
* security auditor,
* institutional risk manager,
* skeptical buyer,
* price-sensitive buyer,
* multilingual,
* accessibility user,
* hostile customer,
* malicious customer,
* low-data customer,
* high-volume customer,
* user comparing alternatives.

---

# 30. PURCHASE INTENT

Each simulated customer must independently classify:

```text
WOULD BUY
WOULD NOT BUY
UNCLEAR
```

and:

```text
WHY?
```

Do not force positive answers.

Separate:

`SIMULATED BUYABILITY`

from:

`REAL MARKET VALIDATION`.

---

# 31. PDF BUYABILITY

Test whether customers understand why a PDF is worth paying for.

Evaluate:

* completeness,
* clarity,
* evidence,
* actionability,
* trust,
* professional quality,
* difference from free view.

If value is weak:

## IMPROVE THE PRODUCT.

---

# 32. PAYMENT SYSTEM

Test full lifecycle:

```text
pricing
→ checkout
→ payment
→ webhook
→ signature verification
→ entitlement
→ server gate
→ feature
```

Then:

* duplicate webhook,
* replay,
* cancellation,
* refund,
* expiration,
* upgrade,
* downgrade,
* payment failure.

Never trust client-side tier state.

Use the current official Stripe webhook verification mechanism and raw request body handling as required by Stripe's documentation.

---

# 33. COMMERCIAL / MERCHANT READINESS

Search for current:

* legal entity data,
* business address,
* contact details,
* tax/VAT handling,
* terms,
* privacy policy,
* cancellation,
* refund,
* withdrawal rights where applicable,
* subscription conditions,
* pricing display,
* invoices,
* AI disclosure,
* risk disclaimer,
* provider attribution,
* accessibility statement.

If required data is missing:

## IMPLEMENT THE TECHNICAL GATE.

Do not allow production checkout while necessary merchant prerequisites are missing.

Do not invent legal identity information.

Mark:

`LEGAL REVIEW REQUIRED`

when human legal review is genuinely required.

---

# 34. ACCESSIBILITY

Test:

* keyboard,
* focus,
* labels,
* forms,
* buttons,
* modals,
* tables,
* payment,
* login,
* error messages,
* responsive,
* basic screen-reader semantics.

Fix actual accessibility blockers.

---

# 35. PERFORMANCE

Measure:

* first load,
* route transitions,
* API latency,
* provider calls,
* retries,
* polling,
* re-renders,
* memory,
* CPU,
* large tables,
* PDF generation.

Find actual causes of lag.

Fix real problems.

Do not perform cosmetic optimization.

---

# 36. VISUAL STABILITY

Do NOT redesign the product.

Preserve the existing identity.

Only fix actual problems:

* blank table,
* missing value,
* broken graph,
* clipping,
* overflow,
* modal failure,
* broken scroll,
* loading glitch,
* animation jank,
* invisible text,
* layout shift.

Test:

375
768
1024
1440

---

# 37. TABLE MASTER ACCEPTANCE

Every major table must be tested.

Minimum:

```text
Shield
Shield Pro
Real Markets
Audit evidence
Tier comparison
```

For each:

* rows,
* cells,
* text,
* numbers,
* source,
* timestamp,
* screenshot,
* desktop,
* mobile.

Fail if:

* empty,
* undefined,
* NaN,
* incorrect,
* disappears,
* breaks after refresh.

---

# 38. PROVIDER FAILURE TESTING

For every critical provider simulate:

* timeout,
* 429,
* 500,
* malformed data,
* schema drift,
* stale data,
* empty response,
* network offline,
* contradictory source.

Verify:

```text
provider state
fallback
evidence
confidence
customer messaging
```

---

# 39. TENANT / AUTH ADVERSARIAL

Attempt:

```text
A → B data
A → B PDF
A → B case
A → B evidence
A → B subscription
A → B entitlement
A → B chat
```

Attempt:

* forged account ID,
* forged cookies,
* altered JWT,
* missing session,
* expired session,
* malicious query params.

Must fail safely.

---

# 40. PAYMENT ADVERSARIAL

Attempt:

* forged product ID,
* forged price,
* forged tier,
* fake webhook,
* old webhook,
* duplicate webhook,
* replay,
* refund manipulation,
* unauthorized entitlement.

Must fail safely.

---

# 41. AI ADVERSARIAL

Attempt:

* prompt injection,
* system prompt extraction,
* secret extraction,
* cross-tenant extraction,
* fabricated source,
* fabricated number,
* false probability,
* jailbreak,
* financial advice,
* leverage,
* fake identity,
* role confusion.

---

# 42. FINAL THREE-ROUND LOOP

### ROUND 1

Run all auditors + customers.

### ROUND 2

Fix every technical problem.

### ROUND 3

Run all auditors + customers again.

If there are still CRITICAL/HIGH or material VALUE GAPS:

repeat.

---

# 43. REAL CUSTOMER FLOW

At least one full journey must work:

```text
LANDING
→ SIGN UP / LOGIN
→ PRODUCT
→ SEARCH
→ DATA
→ ANALYSIS
→ EVIDENCE
→ PDF
→ PRICING
→ CHECKOUT
→ ENTITLEMENT
→ PRO FEATURE
→ RETURN TO PRODUCT
```

Use test payment infrastructure only.

Never pretend a payment happened if it did not.

---

# 44. FINAL PRODUCTION-SAFE CLEAN RESTART

After all fixes:

1. stop server,
2. verify port,
3. start new server,
4. build,
5. TypeScript,
6. lint,
7. critical test suite,
8. provider tests,
9. customer tests,
10. auditor tests,
11. PDF,
12. payment sandbox,
13. mobile,
14. accessibility,
15. security.

Then restart once more and re-run critical flows.

---

# 45. FINAL REPORTS

Generate:

```text
FINAL_SYSTEM_COMPLETION_REPORT.md
FINAL_PROVIDER_RIGHTS_MATRIX.md
FINAL_SECURITY_REPORT.md
FINAL_CUSTOMER_VALUE_REPORT.md
FINAL_AI_AUDITOR_REPORT.md
FINAL_AI_CUSTOMER_REPORT.md
FINAL_TIER_VALUE_MATRIX.md
FINAL_PAYMENT_READINESS.md
FINAL_COMMERCIAL_READINESS.md
FINAL_PRELAUNCH_CHECKLIST.md
```

Update:

```text
MASTER_INDEX.md
testybproadv.md
```

---

# 46. FINAL EVIDENCE DIRECTORY

Create:

```text
artifacts/final_system/
├── runtime/
├── providers/
├── rights/
├── security/
├── risk/
├── ai/
├── auditors/
├── customers/
├── payments/
├── pdf/
├── screenshots/
├── accessibility/
└── performance/
```

Every important result must point to evidence.

---

# 47. FINAL MATRIX

Create:

| Product | Basic | Pro | Advanced | Live Data | Provider | Rights | Risk | Evidence | Security | PDF | Customer Value | Payment | Sales |
| ------- | ----- | --- | -------- | --------- | -------- | ------ | ---- | -------- | -------- | --- | -------------- | ------- | ----- |

Use:

`GO`
`CONDITIONAL`
`NO-GO`

Do NOT hide product-specific blockers behind a global GO.

---

# 48. FINAL SALES DECISIONS

Issue separate decisions:

```text
Security Audit Basic
Security Audit Pro
Security Audit Advanced

Shield Basic
Shield Pro

Real Markets Basic
Real Markets Pro

Browser/Lens Basic
Browser/Lens Pro

Angel
```

For every product:

# SALES GO

# CONDITIONAL GO

# SALES NO-GO

---

# 49. ADVANCED RULE

If Advanced requires:

* human reviewer,
* auditor contract,
* institutional service,
* regulatory approval,
* external licensing,

then:

`NOT_FOR_SALE`

until the requirement is genuinely fulfilled.

AI can test the workflow.

AI cannot pretend to be the required human reviewer.

---

# 50. COMMERCIAL TRUTH

The final report must explicitly state:

```text
WHAT CAN BE SOLD TODAY
WHAT CAN BE USED FOR FREE
WHAT REQUIRES A PAID PROVIDER
WHAT REQUIRES A COMMERCIAL LICENSE
WHAT REQUIRES ATTRIBUTION
WHAT REQUIRES HUMAN REVIEW
WHAT REQUIRES LEGAL REVIEW
WHAT REQUIRES A BUSINESS ENTITY SETUP
WHAT IS TECHNICALLY COMPLETE
WHAT IS NOT COMPLETE
```

No marketing language.

---

# 51. FINAL CUSTOMER VALUE QUESTION

For every paid product:

> "What exactly would I pay VELMÈRE for?"

The answer must mention concrete functionality.

Not:

`advanced analytics`

but:

```text
60-level orderbook
$10k slippage simulation
specific evidence
specific report
specific data coverage
specific risk analysis
```

where those capabilities actually exist.

---

# 52. FINAL RELEASE CONDITIONS

A product can receive SALES GO only when:

```text
FUNCTIONALITY = PASS
DATA = PASS
PROVIDER = PASS
RIGHTS = PASS
SECURITY = PASS
ENTITLEMENT = PASS
CUSTOMER FLOW = PASS
VALUE = PASS
PDF = PASS
PAYMENT = PASS
COMMERCIAL PREREQUISITES = PASS
```

External legal/human dependencies must be explicitly satisfied or explicitly excluded from the product promise.

---

# 53. MOST IMPORTANT RULE

DO NOT MAKE TESTS PASS BY CHEATING.

Do not:

* weaken assertions,
* bypass auth,
* bypass tier gates,
* bypass rights,
* disable security,
* rename mock data,
* rename simulation as live,
* hide failures,
* delete failing tests.

Instead:

# FIX THE PRODUCT.

---

# 54. END STATE

When finished, a completely new engineer should be able to clone/open the repository and understand:

```text
WHAT VELMÈRE DOES
WHAT DATA IT USES
WHERE DATA COMES FROM
WHAT RIGHTS APPLY
WHAT BASIC DOES
WHAT PRO DOES
WHAT ADVANCED DOES
WHAT IS LIVE
WHAT IS FALLBACK
WHAT IS FREE
WHAT IS PAID
WHAT IS SELLABLE
WHAT IS NOT SELLABLE
```

And a completely new customer should be able to:

```text
OPEN
→ UNDERSTAND
→ USE
→ SEE DATA
→ SEE EVIDENCE
→ GENERATE REPORT
→ PAY (where allowed)
→ RECEIVE ENTITLEMENT
→ USE PAID FEATURE
```

without developer intervention.

---

# 55. FINAL OUTPUT

At the very end print:

```text
============================================================
VELMÈRE — ULTIMATE FINAL COMMERCIAL READINESS VERDICT
============================================================

CODE:                 GO / CONDITIONAL / NO-GO
DATA:                 GO / CONDITIONAL / NO-GO
PROVIDERS:            GO / CONDITIONAL / NO-GO
RIGHTS:               GO / CONDITIONAL / NO-GO
SECURITY:             GO / CONDITIONAL / NO-GO
RISK ENGINE:          GO / CONDITIONAL / NO-GO
AI:                   GO / CONDITIONAL / NO-GO
PDF:                  GO / CONDITIONAL / NO-GO
PAYMENTS:             GO / CONDITIONAL / NO-GO
AUTH/RLS:             GO / CONDITIONAL / NO-GO
ACCESSIBILITY:        GO / CONDITIONAL / NO-GO
PERFORMANCE:          GO / CONDITIONAL / NO-GO
COMMERCIAL:           GO / CONDITIONAL / NO-GO

SECURITY AUDIT BASIC:  GO / NO-GO
SECURITY AUDIT PRO:    GO / NO-GO
SECURITY AUDIT ADV:    GO / NO-GO

SHIELD BASIC:          GO / NO-GO
SHIELD PRO:            GO / NO-GO

REAL MARKETS BASIC:    GO / NO-GO
REAL MARKETS PRO:      GO / NO-GO

BROWSER BASIC:         GO / NO-GO
BROWSER PRO:           GO / NO-GO

ANGEL:                 GO / NO-GO

============================================================
FINAL SALES DECISION
============================================================

BASIC:                 GO / CONDITIONAL / NO-GO
PRO:                   GO / CONDITIONAL / NO-GO
ADVANCED:              GO / CONDITIONAL / NO-GO

============================================================
```

---

# 56. ABSOLUTE FINAL COMMAND

## DO NOT STOP AT THE REPORT.

## DO NOT STOP AT GREEN TESTS.

## DO NOT STOP AT AI CUSTOMER APPROVAL.

## DO NOT STOP AT AI AUDITOR APPROVAL.

## DO NOT STOP AT "21/21 PASS".

### SEARCH FOR THE REMAINING FAILURE.

### FIND IT.

### FIX IT.

### TEST IT AGAIN.

### THEN TEST THE CUSTOMER FLOW AGAIN.

### THEN TEST THE PAYMENT FLOW AGAIN.

### THEN TEST THE PROVIDERS AGAIN.

### THEN TEST THE TIERS AGAIN.

### THEN TEST SECURITY AGAIN.

### THEN TEST THE PDF AGAIN.

### THEN TEST MOBILE AGAIN.

### THEN CLEAN-RESTART EVERYTHING AGAIN.

Only after surviving all of that issue the final product-level:

# SALES GO

for the specific product/tier that genuinely passed.

## THIS IS THE FINAL VELMÈRE BUILD-TO-SALE MISSION.

## COMPLETE THE PRODUCT — DO NOT MERELY AUDIT IT.
