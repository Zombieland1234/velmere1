PASS35 A54 — STRICT FAIL-CLOSED SLO / ALERT-ACK / VENDOR-EXIT / RECOVERY HARDENING

- Supersedes A53 for future staging execution.
- Stops before the first request when any runtime, evidence, confirmation, URL, DNS, secret or source-anchor precondition fails.
- Adds strict numeric schemas without Number(null) coercion, current bounded SLO windows, count algebra and percentile ordering.
- Adds exact run/revision/source/nonce/step-chain binding to every request and response.
- Separates alert trigger, alert observer and ACK actor credentials; separates vendor control and observer credentials.
- Requires multi-domain alternate-provider identity and a negative probe proving the old primary credential is rejected.
- Adds per-run lock, durable journal, compensating restore in finally, retry and RECOVERY_REQUIRED decision.
- Evidence packaging refuses fixture, failed, unrecovered or non-VERIFIED receipts.
- Local adversarial suite covers 61 scenarios / 292 assertions.
- No UI, asset, protected-engine, tier, price, sale or LIVE change.
