PASS35 A51 — BACKUP / RESTORE / ROLLBACK / PROVIDER-LOSS ACCEPTANCE

Adds fail-closed disposable-staging acceptance tooling for:
- bounded encrypted application-state backup and checksum verification;
- restore into a disposable restore target;
- database/storage digest parity;
- RLS and tenant-isolation revalidation;
- deployment rollback and forward restoration;
- controlled primary-provider outage and failover;
- cleanup, source immutability and evidence redaction.

No UI, public assets, product-tier logic, protected engine, stop-sell or sale state is changed.
Real staging execution is required before any staging or production credit.
