# PASS_02 ACCESSIBILITY AUDIT: WCAG 2.1 AA

## 1. Compliance
- Warning banners include `role="alert"` for screen readers.
- All simulation sequence steps use ordered list `<ol>` semantics with readable tab stops.
