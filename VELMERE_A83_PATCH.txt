PASS36 A83R0 — BROWSER / LENS / PDF REAL-PACKET MATRIX + SECURE DELIVERY PARITY

- Expands the physical PDF denominator from one locale per case to every 50 cases x 3 locales x 3 tiers.
- Generates 450 deterministic PDFs and 2100 A4 pages with exact 2/4/8 page contracts.
- Binds one locale-independent facts digest across PL/EN/DE.
- Binds API, UI, preview, PDF, Brain and Angel to the same facts, report and claim-set digests with zero added facts.
- Rebuilds every report and requires byte-identical PDF output.
- Rejects active PDF content, JavaScript, Launch, remote actions, embedded files, forms, XFA, encryption and external URI actions.
- Verifies embedded fonts, ToUnicode, A4 geometry, external parsing, text extraction and full raster content.
- Runs 8100 resealed semantic mutations and closes independent case-identity and PDF artifact-binding gaps.
- Keeps all 50 real-packet slots blocked until rights, provider freshness, production browser, secure delivery, external accessibility and customer comprehension evidence are supplied.
- Preserves NO_GO, LIVE=false and saleEnabled=false.
- Generalizes tightly scoped root inventory classification for future A<number> PATCH/RUN/VERIFY artifacts; arbitrary VELMERE root files remain UNKNOWN.
- Verifies the immutable A81 -> A82 -> A83 descendant chain without recomputing frozen historical payloads against the current tree.
- Resolves the live completion-program path through current authority, rebuilds the 1073-row protected-integrity receipt on A83 bytes, and advances canonical roadmap precedence to A83.
- Resolves the A77 current completion program through current-revision authority instead of a superseded A82 path.
- Distinguishes local synthetic physical PDF execution from real physical customer PDF delivery; customer execution remains false.
- Uses a bounded parallel raster worker pool while preserving the complete 450-document / 2100-page denominator.
- Adds a compact SHA-256-bound SOURCE_ONLY evidence summary for the full corpus manifest, raster receipt, runtime and test receipt kept in MATERIALS.
- SOURCE_ONLY can now verify A83 evidence identities without embedding 450 PDFs; full independent replay still requires MATERIALS.
- Final A83 closed-gap denominator: 31/31.
