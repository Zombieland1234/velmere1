# Velmère R7 material continuation

R7 is the deterministic three-way merge of R5 and R6 over R4. No material product conflict was discarded. The active internal-FINAL authority is reconciled: external pilot remains post-20.

Current execution: two latest merged local runs each produced 51 PASS and one explicit exact-Windows gate, with 52/52 stable classifications and exit codes. Owner-authorized Supabase staging physically proves two GoTrue users/sessions, JWT/RLS isolation, exact PDF persistence, own-account readback, cross-account denial, reconnect, rollback, concurrency, export, erasure, backup/restore, post-restore RLS and deployed HTTP proof.

Browser Basic is not promoted. Its exact remaining blocker is `HOSTED_EXACT_WINDOWS_R7_52_X2_AND_SOURCE_AUTHORITY_BINDING`. The final Windows transport is deterministic, fully hash-bound and contains no placeholders.
