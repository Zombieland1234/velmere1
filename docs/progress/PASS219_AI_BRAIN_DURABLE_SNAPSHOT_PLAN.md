# PASS219 — AI Brain Durable Snapshot Plan

Selected VLM Brain tile now builds a durable snapshot write plan before customer/PDF export. The plan separates source snapshot, case timeline, redaction envelope and export manifest writes.

All stores remain `not_connected` in this artifact. The UI shows the required server-only storage adapter path without pretending binary PDF, durable proof or production evidence is live.

<!-- PASS219 marker: AI Brain Durable Snapshot Plan active. -->
