# PASS1374–1413 · Mega product polish

Realny blok zamiast mikro-passa. Ten etap dokłada widoczne warstwy produktu, nie tylko verifier.

## Zakres 40+ zadań

- VLM Brain truth: confidence cap, missing data honesty, no fake-live copy.
- Shield terminal: chart-owning gestures, modal above header, tier clarity.
- Real Markets: compact source rhythm, mobile cards direction, no random AI line.
- Lens/PDF: mega-pass strip, premium/PDF truth parity, export header.
- Shield Map: why-verdict rail, no price-table/PDF clone, source→fact→signal→conflict→verdict.
- Header/wallet/cart: anchored language/wallet/account, cart bottom sheet rule, read-only wallet boundary.
- Mobile: 44px targets, no horizontal overflow, safe modal scroll ownership.
- CSS architecture: scoped PASS1374–1413 classes instead of global pass noise.

## Release rule

`pass1413.realWorkStandard` must equal `forty_plus_tasks_no_micro_passes` and `totalTaskCount >= 50` before this block can count toward 100%.

## Still not claimed

Full `npm ci -> typecheck -> lint -> build -> Playwright` remains required on a non-timeout Node 24/npm 11 environment.
