# PASS1454–1493 — Runtime Architecture / Mobile Performance / Rectangular Terminal

This is a heavier pass after the user correctly noted that earlier passes were too small.

## Product rule

Public Shield and Real Markets analysis must use one simple rectangular pattern:

1. chart panel,
2. attached Basic / Pro / Advanced rail,
3. source/gaps details as secondary disclosure,
4. no public bubble/orbit overlay until real screenshots prove it is fast and useful.

## Real changes

- Added the PASS1454 runtime architecture contract.
- Hardened DropdownRoot with a visual-viewport max-height budget.
- Tagged header triggers for language, wallet, account and cart.
- Kept cart as a bottom-sheet-only safe-area drawer.
- Added chart wheel/touch/pointer event containment at the shared modal shell.
- Added public-rule attributes that suppress old bubble/orbit modal classes inside the current shell.
- Made mobile Basic/Pro/Advanced a compact three-button rail instead of a tall stack.
- Added Real Markets source/table runtime architecture attributes.
- Added Shield rectangular-modal proof attributes.
- Added reduced-motion and overflow containment overrides.

## Still not claimed

Full Node 24 install, typecheck, lint, build and Playwright screenshots are still not claimed inside the timeout-limited sandbox.
