# VELMÈRE — R6A INTERNAL CUSTOMER FINAL AUTHORITY RECONCILIATION

Date: **2026-08-23**  
Decision: **PASS_AUTHORITY_RECONCILED**  
Scope: **all 20 current customer-facing rows**

## Binding decision

For the current internal closure stage, `Customer FINAL` means a real, owner-authorized end-to-end execution on an owner-controlled non-production runtime. It must use actual product routes, actual test accounts and JWT/session boundaries, real database/storage behavior where required, current authorized input, the exact customer-visible output, current-byte receipts and every applicable row gate.

The required evidence label is:

`OWNER_AUTHORIZED_INTERNAL_CUSTOMER_EXECUTION`

External human/customer proof is not an active prerequisite for the internal `0/20 → 20/20` numerator. It remains a separate later gate for `PILOT_READY`, `GO_PAID`, `LIVE` and `WORLD_CLASS_PROVEN`, unless a later higher-precedence owner authority explicitly marks a specific row as an exception.

## Why this is reconciliation, not requirement reduction

The V4 authority explicitly orders owner-controlled non-production executions using at least two accounts, distinct JWTs, authorized inputs, real routes, real storage/readback, cross-account negatives, the same output the customer sees and exact receipts. It says this pack may support internal `Customer FINAL` and must not be labelled as a real external customer.

The R3 authority independently defines internal Customer FINAL around owner-authorized non-production execution and places external pilot/human proof in a later separate denominator. It also keeps every technical, security, rights, durability, entitlement and final-byte condition in the row-final boolean contract.

Therefore inherited P101 strings such as `REAL_EXTERNAL_CUSTOMER_PROOF` remain preserved only as historical source-map evidence. They are not active internal row blockers after this reconciliation.

## Requirements that remain unchanged

- exact canonical row and supported scope;
- actual customer route and owner-authorized runtime;
- authentication, authorization and two-tenant isolation;
- field/use rights and source identity/currentness;
- truthful missing-data states;
- durable write/readback and restore where required;
- customer-safe output, privacy and secret redaction;
- evidence receipts and entitlement isolation;
- material matched-input value for paid transitions;
- exact current-byte engineering and Windows Server 2025 proof;
- zero open critical findings.

No fixture, mock, local static sweep, HTTP 200, AI persona or report claiming a customer existed earns row-level FINAL.

## Execution order after reconciliation

1. exact R5/R6 current-source transport and exact Node/npm/full `npm ci` execution;
2. exact Windows Server 2025 replay;
3. owner-authorized staging foundation: migrations, two JWTs, RLS, write/readback, concurrency, restart, backup/restore, export/delete and deployed HTTP;
4. Browser Basic as the first `1/20` target;
5. Risk Indicator;
6. Audit Basic with immutable PDF and same-blob account delivery;
7. tier expansion and remaining rows;
8. only after internal `20/20`: customer campaigns, external pilot and independent human gates.

## Credit boundary

This authority reconciliation fixes an impossible sequencing loop. It grants **zero product FINAL credit by itself**. Current numerator remains `0/20` until physical execution satisfies the unchanged row-final contract.
