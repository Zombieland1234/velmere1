PASS36 A59R0 — BUILD GRAPH + ROUTE REGISTRY + CSS BUDGET RECOVERY

Revision: VELMERE_PASS36_A59R0_BUILD_GRAPH_ROUTE_CSS_BUDGET_RECOVERY
Parent: VELMERE_PASS36_A58R0_RELEASE_INTEGRITY_FINAL_BYTE_BINDING

Implemented:
- consolidated public root alias pages into the existing strict-nonce proxy alias registry;
- consolidated endpoint-specific API shells into fail-closed lazy route registries while preserving public paths and methods;
- added nested registries for VLM and Real Markets operations;
- preserved 265 logical API operations and 100% bounded/rejected body handling for mutating operations;
- reduced the static build graph to the existing limits without increasing thresholds;
- pruned only high-confidence unused pass-marked selectors and lexically compacted CSS with parser/hash receipts;
- updated A41 diagnostics for registry-owned routes;
- added an A59 fail-closed verifier and explicit exact-runtime/browser blockers.
- promoted the critical CSS compaction and exact-dedup receipts into source-bound config/pass36 files so A59 self-verifies after clean SOURCE_ONLY unpack; artifacts remain evidence copies only.

Static final candidate targets:
- entrypoints <= 148;
- API routes <= 82;
- aggregate CSS <= 3,400,000 bytes;
- app/globals.css <= 3,100,000 bytes;
- server routes importing client code = 0;
- unresolved local imports = 0.

Truth boundary:
This pass does not claim a fresh exact Node 24.18.0/npm 11.16.0 Webpack/Turbopack build or browser screenshot parity. It does not enable LIVE, production payments or sale.
