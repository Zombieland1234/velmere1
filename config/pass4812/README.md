# PASS4812 configuration boundary

Production configs must contain public keys, identifiers, digests, public URLs and external command adapters only. Never place private PEM data, raw cloud credentials, signing secrets or approver secrets in these files. Prefer a protected deployment workspace outside the source tree; the examples in this repository are documentation only.
