{
  "schemaVersion": "velmere.r10.active-workflow-policy.v1",
  "inventoryMode": "DISCOVER_ALL_ACTIVE_YAML",
  "requiredBaselineWorkflowFiles": [
    "pass4992-supply-chain-security.yml",
    "quality-and-fresh-builds.yml"
  ],
  "forbidUnexpectedWorkflowFailure": true,
  "forbidHardcodedGlobalActionReferenceCount": true,
  "truthBoundary": "Every active .github/workflows/*.yml|*.yaml file is audited. Baseline workflow names are minimum required controls, not an exhaustive allowlist. Adding a workflow changes observed inventory but is not itself a release failure."
}
