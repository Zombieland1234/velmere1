from __future__ import annotations
import argparse,hashlib,json
from pathlib import Path
PARENT={'fileCount': 1601, 'payloadBytes': 21037233, 'pathSetSha256': '40b966b3bc2497a1d1d18b967ec867f182f76030af23d15329e42c6057268d59', 'sourceContentAggregateSha256': '354ec7229eb61dd55cccdae90c4a94576967f1c3beb9bad67909d847ccf1e032'}
EXPECTED={'fileCount': 1601, 'payloadBytes': 21046508, 'pathSetSha256': '40b966b3bc2497a1d1d18b967ec867f182f76030af23d15329e42c6057268d59', 'sourceContentAggregateSha256': 'fa003ce6e6280b4027158f0440cbaebe680578a2fa358ab2e91f3b947cfe8a99'}
SPECS=[{'path': 'lib/security/audit-provider-runtime-client.ts', 'beforeBytes': 39628, 'beforeSha256': '090c7377c7a963395adbd7446339d1a4ef91e68fc59dcee7ab8dc068db107bcd', 'afterBytes': 42332, 'afterSha256': 'af85d1af0a5d84e3760dbda817dad21c958d1ed13e8ecb33a5a43aaa73ab49c1', 'ops': [[8, 9, ['import { parseStrictJsonBytes, parseStrictJsonText } from "@/lib/security/strict-json-boundary";\n']], [117, 117, ['export type Pass2572VerifiedStaticEvidence = {\n', '  contractAddress: string;\n', '  chain: string;\n', '  provider: string;\n', '  observedAt: string;\n', '  responseDigest: string;\n', '  sourceText?: string;\n', '  abiText?: string;\n', '  bytecodeText?: string;\n', '};\n', '\n']], [120, 120, ['  /** Server-private sink. Raw verified source/ABI never enters the public provider report. */\n', '  verifiedStaticEvidenceSink?: (evidence: Pass2572VerifiedStaticEvidence | null) => void;\n']], [367, 368, ['function flattenExplorerSource(sourceCode: string) {\n', '  const trimmed = sourceCode.trim();\n', '  if (!trimmed) return "";\n', '  const candidates = trimmed.startsWith("{{") && trimmed.endsWith("}}")\n', '    ? [trimmed.slice(1, -1), trimmed]\n', '    : [trimmed];\n', '  for (const candidate of candidates) {\n', '    try {\n', '      const parsed = parseStrictJsonText(candidate, {\n', '        maxBytes: 1_600_000,\n', '        maxDepth: 40,\n', '        maxNodes: 80_000,\n', '        requireObject: true,\n', '      }) as Record<string, unknown>;\n', '      const sources = asRecord(parsed.sources);\n', '      if (!sources) continue;\n', '      const rows = Object.entries(sources)\n', '        .sort(([a], [b]) => a.localeCompare(b, "en"))\n', '        .flatMap(([path, value]) => {\n', '          const record = asRecord(value);\n', '          const content = typeof record?.content === "string" ? record.content.trim() : "";\n', '          return content ? [`// source-file: ${path}\\n${content}`] : [];\n', '        });\n', '      if (rows.length) return rows.join("\\n\\n").slice(0, 1_200_000);\n', '    } catch {\n', '      // Plain single-file Solidity is expected for many verified contracts.\n', '    }\n', '  }\n', '  return trimmed.slice(0, 1_200_000);\n', '}\n', '\n', 'async function explorerLane(\n', '  contractAddress: string | undefined,\n', '  chain: string,\n', '  chainId: string,\n', '  locale: string,\n', '  verifiedStaticEvidenceSink?: (evidence: Pass2572VerifiedStaticEvidence | null) => void,\n', '): Promise<Pass2572RuntimeLane> {\n']], [371, 371, ['    verifiedStaticEvidenceSink?.(null);\n']], [387, 387, ['    verifiedStaticEvidenceSink?.(null);\n']], [426, 427, ['  const output = lane({\n']], [466, 466, ['  const responseDigest = sourceResult.bodyDigest;\n', '  if (state === "confirmed" && identityMatched && responseDigest && (sourceCode || abi)) {\n', '    verifiedStaticEvidenceSink?.({\n', '      contractAddress: contractAddress.toLowerCase(),\n', '      chain,\n', '      provider: "Etherscan V2",\n', '      observedAt: sourceResult.observedAt,\n', '      responseDigest,\n', '      sourceText: sourceCode ? flattenExplorerSource(sourceCode) : undefined,\n', '      abiText: abi && abi !== "Contract source code not verified" ? abi : undefined,\n', '    });\n', '  } else {\n', '    verifiedStaticEvidenceSink?.(null);\n', '  }\n', '  return output;\n']], [754, 755, ['    explorerLane(contractAddress, chain, chainId, locale, input.verifiedStaticEvidenceSink),\n']], [862, 862, ['  // A request that asks for server-private raw static evidence must execute the provider lane\n', '  // in this request. Serving only the cached public projection would lose the private evidence.\n', '  if (input.verifiedStaticEvidenceSink) {\n', '    return buildUncachedPass2572AuditProviderRuntimeReport(input);\n', '  }\n']]]}, {'path': 'lib/security/audit-watch-post-handler.ts', 'beforeBytes': 39533, 'beforeSha256': '2f8cfdfd7aa11bf448c2bee00398a72647137e550e52dc0e37de0f6639217c62', 'afterBytes': 39938, 'afterSha256': 'edfe905b97dc74e9e6e42f1252430ec57e9fb6166084425c8d0145109217a6b5', 'ops': [[29, 30, ['import { buildPass2572AuditProviderRuntimeReport, type Pass2572VerifiedStaticEvidence } from "@/lib/security/audit-provider-runtime-client";\n']], [221, 221, ['  let pass2572VerifiedStaticEvidence: Pass2572VerifiedStaticEvidence | null = null;\n']], [225, 225, ['    verifiedStaticEvidenceSink: (evidence) => {\n', '      pass2572VerifiedStaticEvidence = evidence;\n', '    },\n']], [251, 251, ['    verifiedStaticEvidence: pass2572VerifiedStaticEvidence,\n']], [269, 269, ['    verifiedStaticEvidence: pass2572VerifiedStaticEvidence,\n']], [300, 300, ['    verifiedStaticEvidence: pass2572VerifiedStaticEvidence,\n']]]}, {'path': 'lib/security/audit-report-assembler.ts', 'beforeBytes': 26736, 'beforeSha256': '8ca934e68c0d432479f9faf25d5bcd1f54a157abeab80082bc89d9a6f3b171f0', 'afterBytes': 32902, 'afterSha256': 'b2261aed5c2a0eca635ffe581ea17d506df93304988bbcb949fab2e3e10d808a', 'ops': [[3, 4, ['import type { Pass2572AuditProviderRuntimeReport, Pass2572VerifiedStaticEvidence } from "./audit-provider-runtime-client";\n']], [105, 105, ['  verifiedStaticEvidence?: Pass2572VerifiedStaticEvidence | null;\n']], [185, 185, ['function verifiedStaticSourceForReport(\n', '  input: AssemblerInput,\n', '  contractAddress: string | undefined,\n', '  chain: string,\n', ') {\n', '  const evidence = input.verifiedStaticEvidence;\n', '  if (\n', '    !evidence ||\n', '    !contractAddress ||\n', '    !/^0x[a-fA-F0-9]{40}$/.test(evidence.contractAddress) ||\n', '    evidence.contractAddress.toLowerCase() !== contractAddress.toLowerCase() ||\n', '    evidence.chain.trim().toLowerCase() !== chain.trim().toLowerCase() ||\n', '    !evidence.provider.trim() ||\n', '    !Number.isFinite(Date.parse(evidence.observedAt)) ||\n', '    !/^(?:sha256:)?[a-fA-F0-9]{64}$/.test(evidence.responseDigest)\n', '  ) return null;\n', '  const source = typeof evidence.sourceText === "string" ? evidence.sourceText.slice(0, 1_200_000) : "";\n', '  if (!/\\b(?:contract|library)\\s+[A-Za-z_][A-Za-z0-9_]*/.test(source) || !source.includes("{")) return null;\n', '  return { source, provider: evidence.provider, responseDigest: evidence.responseDigest.replace(/^sha256:/, "") };\n', '}\n', '\n', 'function stripSolidityForOrdering(source: string) {\n', '  return source\n', '    .replace(/\\/\\*[\\s\\S]*?\\*\\//g, (value) => value.replace(/[^\\n]/g, " "))\n', '    .replace(/\\/\\/[^\\n\\r]*/g, (value) => " ".repeat(value.length))\n', '    .replace(/"(?:\\\\.|[^"\\\\])*"/g, (value) => value.replace(/[^\\n]/g, " "))\n', '    .replace(/\'(?:\\\\.|[^\'\\\\])*\'/g, (value) => value.replace(/[^\\n]/g, " "));\n', '}\n', '\n', 'function matchingBrace(source: string, open: number) {\n', '  let depth = 0;\n', '  for (let index = open; index < source.length; index += 1) {\n', '    if (source[index] === "{") depth += 1;\n', '    else if (source[index] === "}") {\n', '      depth -= 1;\n', '      if (depth === 0) return index;\n', '    }\n', '  }\n', '  return -1;\n', '}\n', '\n', 'type StaticVulnerabilityResult = {\n', '  findings: Pass2578TopFinding[];\n', '  riskFloor: number | null;\n', '  advancedActions: string[];\n', '};\n', '\n', '/**\n', ' * Bounded customer-safe source scan. It detects value-bearing external interaction\n', ' * before an indexed/member state mutation in the same Solidity function. It does\n', ' * not produce exploit payloads or claim exploitability from static source alone.\n', ' */\n', 'function staticVulnerabilityFindings(\n', '  input: AssemblerInput,\n', '  contractAddress: string | undefined,\n', '  chain: string,\n', '  locale: string,\n', '): StaticVulnerabilityResult {\n', '  const verified = verifiedStaticSourceForReport(input, contractAddress, chain);\n', '  if (!verified) return { findings: [], riskFloor: null, advancedActions: [] };\n', '  const source = stripSolidityForOrdering(verified.source);\n', '  const functionStart = /\\bfunction\\s*(?:[A-Za-z_][A-Za-z0-9_]*)?\\s*\\([^)]*\\)[^{;]*\\{/g;\n', '  const interaction = /\\.call\\s*\\{[^}]*\\bvalue\\s*:|\\.call\\.value\\s*\\(|\\.(?!call\\b|callcode\\b)[A-Za-z_][A-Za-z0-9_]*\\.value\\s*\\(/g;\n', '  const indexedOrMemberMutation = /\\b[A-Za-z_][A-Za-z0-9_]*(?:\\s*\\[[^\\]\\n]+\\])+\\s*(?:\\+=|-=|\\*=|\\/=|%=|=|\\+\\+|--)|\\b[A-Za-z_][A-Za-z0-9_]*(?:\\s*\\[[^\\]\\n]+\\])*\\.[A-Za-z_][A-Za-z0-9_]*\\s*(?:\\+=|-=|\\*=|\\/=|%=|=|\\+\\+|--)/g;\n', '  let match: RegExpExecArray | null;\n', '  while ((match = functionStart.exec(source))) {\n', '    const open = source.indexOf("{", match.index);\n', '    if (open < 0) continue;\n', '    const close = matchingBrace(source, open);\n', '    if (close < 0) continue;\n', '    const body = source.slice(open + 1, close);\n', '    interaction.lastIndex = 0;\n', '    const call = interaction.exec(body);\n', '    if (!call) continue;\n', '    indexedOrMemberMutation.lastIndex = call.index + call[0].length;\n', '    const mutation = indexedOrMemberMutation.exec(body);\n', '    if (!mutation) continue;\n', '    const absoluteOffset = open + 1 + call.index;\n', '    const line = source.slice(0, absoluteOffset).split("\\n").length;\n', '    const digestPrefix = verified.responseDigest.slice(0, 16);\n', '    const finding: Pass2578TopFinding = {\n', '      id: `finding-static-reentrancy-order-${line}`,\n', '      severity: "elevated",\n', '      title: t(locale, "Możliwe ryzyko kolejności reentrancy · verified source", "Moegliches Reentrancy-Reihenfolge-Risiko · verified source", "Potential reentrancy ordering risk · verified source"),\n', '      publicLine: t(\n', '        locale,\n', '        "Zweryfikowany source zawiera zewnętrzne wywołanie z wartością przed późniejszą zmianą stanu. Wymaga potwierdzenia ścieżki i retestu po poprawce; sam sygnał statyczny nie dowodzi exploita.",\n', '        "Verified Source enthaelt einen externen Value-Call vor einer spaeteren Zustandsaenderung. Pfad bestaetigen und nach Fix erneut testen; das statische Signal beweist allein keinen Exploit.",\n', '        "Verified source contains a value-bearing external call before a later state mutation. Confirm the reachable path and retest after remediation; the static signal alone does not prove exploitability.",\n', '      ),\n', '      proLine: `source-bound static ordering signal; provider=${verified.provider}; responseDigest=${digestPrefix}…; line=${line}; exploitability=unproven`,\n', '      advancedAction: t(\n', '        locale,\n', '        "Potwierdzić callback/re-entry reachability, przenieść krytyczne effects przed interaction lub zastosować odpowiedni guard i wykonać deterministyczny retest wersji poprawionej.",\n', '        "Callback/Re-entry-Reachability bestaetigen, kritische Effects vor Interaction verschieben oder passenden Guard einsetzen und die korrigierte Version deterministisch erneut testen.",\n', '        "Confirm callback/re-entry reachability, move critical effects before the interaction or apply an appropriate guard, then deterministically retest the remediated version.",\n', '      ),\n', '      sourceFamily: verified.provider,\n', '    };\n', '    return { findings: [finding], riskFloor: 72, advancedActions: [finding.advancedAction] };\n', '  }\n', '  return { findings: [], riskFloor: null, advancedActions: [] };\n', '}\n', '\n']], [254, 254, ['  const staticVulnerability = staticVulnerabilityFindings(input, contractAddress, chain, locale);\n']], [382, 383, ['  const verifiedRiskFloors = [adverseRiskFloor, staticVulnerability.riskFloor]\n', '    .filter((value): value is number => value !== null);\n', '  const verifiedRiskFloor = verifiedRiskFloors.length ? Math.max(...verifiedRiskFloors) : null;\n', '  const finalRisk = verifiedRiskFloor === null\n']], [385, 387, ['      ? verifiedRiskFloor\n', '      : Math.max(riskScore, verifiedRiskFloor);\n']], [402, 403, ['  const topFindings = [...staticVulnerability.findings, ...adverseFindings, ...sectionFindings]\n']], [409, 410, ['    .concat(staticVulnerability.advancedActions, input.permissionParser?.advancedQueue ?? [], input.liquidityHolderRisk?.advancedQueue ?? [])\n']], [419, 419, ['    `Verified static vulnerability findings: ${staticVulnerability.findings.length}; staticRiskFloor=${staticVulnerability.riskFloor ?? "none"}`,\n']]]}]
def sha(b:bytes):return hashlib.sha256(b).hexdigest()
def identity(rows):
 rows=sorted(rows,key=lambda r:r['path']);h=hashlib.sha256();ps=hashlib.sha256('\n'.join(r['path'] for r in rows).encode()).hexdigest()
 for r in rows:h.update(f"{r['path']}\0{r['byteLength']}\0{r['sha256']}\n".encode())
 return {'fileCount':len(rows),'payloadBytes':sum(int(r['byteLength']) for r in rows),'pathSetSha256':ps,'sourceContentAggregateSha256':h.hexdigest()}
ap=argparse.ArgumentParser();ap.add_argument('--source-root',required=True);ap.add_argument('--parent-manifest',required=True);ap.add_argument('--manifest',required=True);ap.add_argument('--receipt',required=True);a=ap.parse_args();root=Path(a.source_root);m=json.loads(Path(a.parent_manifest).read_text(encoding='utf-8'))
for k,v in PARENT.items():
 if m['projection'].get(k)!=v:raise SystemExit(f"P78R2 parent projection mismatch:{k}:{m['projection'].get(k)}:{v}")
rowmap={r['path']:dict(r) for r in m['files']};changed=[]
for spec in SPECS:
 p=root/spec['path'];b=p.read_bytes()
 if len(b)!=spec['beforeBytes'] or sha(b)!=spec['beforeSha256']:raise SystemExit(f"P78R2 preimage mismatch:{spec['path']}:{len(b)}:{sha(b)}")
 lines=b.decode('utf-8').splitlines(keepends=True);out=[];cursor=0
 for i1,i2,newlines in spec['ops']:
  if i1<cursor:raise SystemExit(f"P78R2 patch overlap:{spec['path']}")
  out.extend(lines[cursor:i1]);out.extend(newlines);cursor=i2
 out.extend(lines[cursor:]);raw=''.join(out).encode('utf-8')
 if len(raw)!=spec['afterBytes'] or sha(raw)!=spec['afterSha256']:raise SystemExit(f"P78R2 output mismatch:{spec['path']}:{len(raw)}:{sha(raw)}")
 p.write_bytes(raw);rowmap[spec['path']]['byteLength']=len(raw);rowmap[spec['path']]['sha256']=sha(raw);changed.append({k:spec[k] for k in ['path','beforeBytes','beforeSha256','afterBytes','afterSha256']})
rows=sorted(rowmap.values(),key=lambda r:r['path']);observed=identity(rows)
if observed!=EXPECTED:raise SystemExit(f'P78R2 projection mismatch:{observed} != {EXPECTED}')
new=dict(m);new['schemaVersion']='velmere.p78r2.build-relevant-projection.v1';new['classification']='CURRENT_PRODUCT_PROJECTION_P78R2_VERIFIED_STATIC_EVIDENCE_CUSTOMER_AUDIT';new['projection']=dict(m['projection']);new['projection'].update(EXPECTED);new['projection']['purpose']='Exact Windows proof for server-private verified explorer source/ABI propagation into the customer Audit analysis and bounded static reentrancy-order finding generation.';new['projection']['excludedFromCredit']=['production deployment','real customer FINAL','Audit FINAL PDF','rights expansion','paid value','sale eligibility','LIVE','world-class proof'];new['files']=rows;new['p78r2Delta']={'parent':'P77R3','changedBuildRelevantFiles':changed,'repair':'Keep exact-identity explorer source/ABI server-private, propagate it into existing permission/source extraction, and generate bounded customer-safe static reentrancy ordering findings without exploitability overclaim.','customerFinalOutputCredit':0,'auditFinalPdfCredit':0,'rightsCredit':0,'paidValueCredit':0,'saleCredit':0,'live':False}
Path(a.manifest).write_text(json.dumps(new,indent=2)+'\n',encoding='utf-8');receipt={'schemaVersion':'velmere.p78r2.verified-static-evidence-source-patch.v1','status':'PASS','parentProjection':PARENT,'projection':observed,'changedFiles':changed,'securityBoundary':{'rawSourcePublicReport':False,'identityBoundRequired':True,'responseDigestRequired':True,'staticSignalExploitabilityClaimed':False},'zeroFakeCredit':{'customerFinal':'0/20','auditFinalPdf':'0/3','rights':'2/203','paidValue':'0/10','saleEligible':'0/20','live':False}};Path(a.receipt).write_text(json.dumps(receipt,indent=2)+'\n',encoding='utf-8');print(json.dumps(receipt,indent=2))
