# VELMÈRE — P101R1 ZERO‑EURO FULL PRODUCT ACTIVATION, 20/20 CUSTOMER FINAL & AI VALIDATION MASTER OVERRIDE V4

DATE: 2026-08-21  
OWNER EXECUTION OVERRIDE  
TARGET ENVIRONMENT: Codex / local Windows + authorized non-production tools  
CURRENT CANONICAL START: P101R1

## COMPLETENESS CONTRACT

- Expected numbered sections: 0 through 72 inclusive.
- Expected numbered section count: 73.
- Final numbered section: 72. FINAL OWNER RULE.
- After section 72 the file must contain `START NOW` and `END-OF-DIRECTIVE`.
- If any section is missing or the final sentinel is absent, classify this instruction as `TRUNCATED_OWNER_DIRECTIVE` and do not invent missing text.
- Before material work, explicitly verify: `SECTIONS 0–72 PRESENT`.

======================================================================

# 0. AUTHORITY AND PURPOSE

You continue the implementation of **Velmère** from the latest canonical checkpoint **P101R1**.

The following remain higher-order authorities:

1. **Master Directive V2 R1 — current master execution authority**
2. **Canonical Owner Directive V17**
3. **P101R1 Current State & Pass Delta Ledger**
4. **P101R1 SOURCE_ONLY and its exact receipts**

This V4 prompt is a new explicit owner execution override. It does not cancel Master V2 R1 or V17. It clarifies the order and scope of the next work:

- make every actual product surface function end-to-end;
- make Shield, Shield Pro and Real Markets tables work correctly;
- make Audit Basic/Pro/Advanced and Browser Basic/Pro/Advanced work;
- execute full AI customer, AI auditor, AI reviewer and adversarial campaigns now;
- use free/open/public/free-tier sources first;
- build the missing staging and runtime proof wherever an authorized free path exists;
- pursue `Customer FINAL 0/20 → 20/20` without fake credit;
- continue through all technically executable global gates toward `FINAL_CANDIDATE`.

A checkpoint is a savepoint, not the goal.

# 1. EXACT CANONICAL START

Expected authority:

**Master Directive V2 R1 SHA-256**

`45e2b377be0869f1acb3ef34844919643a537c617b0a8145402cc8c732e8b3c1`

**Canonical Owner Directive V17 SHA-256**

`de7fbd6df651a7a8a8f85054c6e9ac3c2ca530ebfb0d4ccd335dcb99fb658f05`

Current checkpoint:

**P101R1**

- parent: P99R1;
- zero inheritance from abandoned/non-canonical P100;
- status: `PASS_BOUNDED_SAVEPOINT_0_OF_20`;
- global status: `NO_GO / STOP_SELL`.

Expected **P101R1 SOURCE_ONLY ZIP**:

- bytes: `216245335`
- SHA-256:
  `c2b7ab7a924fa87265518246a9bbac9da44f674e72c776da21b5ea1b4040ed3d`
- entries: `9636`
- manifest rows: `9634`
- manifest SHA-256:
  `ee5e9f8245601c472c3f753c21919fbbd3870f39f1e6bb5d623bdc4be1e9031e`
- tree excluding self: `9635 files / 494219718 B`
- path-set SHA-256:
  `ffe0b53613424932a67291994632fdced94e2c1f0e645df950ff4b57d633ff1e`
- content aggregate:
  `56d0461b2ad99d044728d15e68215c39c494ba4a19150195cfbe8d1a2e0cf9ce`
- product/source projection:
  `7319 files / 208488813 B`
- product/source projection SHA-256:
  `78f56e9bf906fa5181d7db289ca1aa96f9eda102549a4898bed11184f0b1e4d2`

Expected **P101R1 ledger SHA-256**:

`1f9f3b0a6c9c988be20110c9c18029d65026b591b4668242bf841f57f09f21e1`

Expected Git binding:

- commit:
  `978fd9c58eee6e4ee6b0affe514c7dadd918b5f8`
- tree:
  `94a269e987ecb629f932d3bb35467432a56dd15b`
- tag:
  `local-checkpoint-p101r1-verified`
- tag object:
  `3615566463aead213272be6203fbfd5ba7b5057a`

Verify all available exact values before promotion. If an input differs, do not guess and do not silently substitute another branch.

# 2. OWNER INTERPRETATION CORRECTION — INTERNAL FINAL VS EXTERNAL PILOT

P101R1 treated “external customer execution” too broadly as a blocker for many internal Customer FINAL rows.

Apply this owner clarification:

## Internal `Customer FINAL`

A customer-facing row may receive internal `FINAL` when it has a complete, real, authorized end-to-end execution using:

- an owner-authorized non-production environment;
- actual test accounts and JWT/session boundaries;
- actual product routes;
- an owner-authorized real or historical input;
- real storage/readback where required;
- rights-safe/current evidence where required;
- the final customer-visible output;
- exact bytes/hash where required;
- current regression and independent adjudication.

The internal executing account may be an owner-controlled staging customer account. It does not have to be an unrelated outside company.

## Real external proof

Actual third-party customers, external pilot participants and independent human reviewers remain a separate denominator:

`REAL_EXTERNAL_PROOF_REQUIRED`

They are required for the appropriate pilot, external-validation, `GO_PAID`, `LIVE` or `WORLD_CLASS_PROVEN` gates, but they are not automatically a prerequisite for every internal Customer FINAL row unless V17 explicitly requires that exact external proof for that row.

Do not use this clarification to weaken rights, security, currentness, product truth, staging, storage or customer-output requirements.

# 3. OWNER OVERRIDE — AI CAMPAIGNS START NOW

Do not wait for 20/20 before running AI-simulated customer, auditor, reviewer and adversarial campaigns.

Execute them in two layers:

1. **development/baseline campaigns now** on each stable runnable product surface to find real defects;
2. **final frozen-candidate campaigns later** after all relevant fixes, on exact current bytes.

AI-simulated evidence:

- may discover bugs;
- may provide `AI_SIMULATED_PASS_BOUNDED`;
- may support internal quality closure;
- must never be labeled as a real human customer, external customer, independent human audit or real pilot.

Do not end a session by saying “only external actions remain” while any required AI-simulated campaign from this directive is still executable.

# 4. OWNER DECISION — SHIELD PRO TOPOLOGY IS CONFIRMED

The topology is not ambiguous.

Use exact canonical product identifiers:

- `audit.basic`
- `audit.pro`
- `audit.advanced`
- `browser.basic`
- `browser.pro`
- `browser.advanced`
- `shield.basic`
- `shield.pro`
- `shield.advanced`
- `shield_pro.basic`
- `shield_pro.pro`
- `shield_pro.advanced`
- `real_markets.basic`
- `real_markets.pro`
- `real_markets.advanced`
- `shield_map`
- `market_impact`
- `whale_watch`
- `angel`
- `risk_indicator`

`shield.pro` means the Pro tier of the Shield family.

`shield_pro.*` means the separate Shield Pro product family.

Shield Pro is a real tiered product with Basic/Pro/Advanced and must have a real server-bound customer path, actual tables/workflows and material tier value. Do not leave its topology as an owner decision.

# 5. TRUE PRODUCT DENOMINATORS

Current truth:

- 10 product families;
- 20 customer-facing rows;
- 20 execution profiles;
- 10 material paid transitions.

Tiered:

- Audit B/P/A
- Browser B/P/A
- Shield B/P/A
- Shield Pro B/P/A
- Real Markets B/P/A

Standalone:

- Shield Map
- Market Impact — FREE
- Whale Watch — FREE
- Angel — one AI brain/chat
- Risk Indicator

PDF is an artifact/output, not a product row.

Do not create artificial tiers for standalone products.

# 6. PRIMARY MISSION

Continue until all authorized, executable, material work is exhausted.

Primary product target:

# `Customer FINAL 0/20 → 20/20`

Then continue through all technically executable global gates toward:

# `FINAL_CANDIDATE`

Do not stop after:

- one fix;
- one checkpoint;
- one product;
- one AI campaign;
- `1/20`;
- `20/20` while global gates remain open.

# 7. ZERO-EURO CLOSURE STRATEGY

Build the product as far as reasonably possible for **0 EUR before launch**.

Provider preference order:

1. official public/open data;
2. open-license datasets;
3. direct public chain data through rights-safe read-only access;
4. free endpoint without a key;
5. free API key;
6. free-tier API;
7. existing included/free infrastructure quota;
8. paid provider only as `OWNER_DECISION_CANDIDATE`.

Do not:

- buy an API;
- start a paid subscription;
- accept a paid upgrade;
- spend beyond an existing included/free quota;
- create a paid external account;
- activate production billing

without explicit owner approval.

A missing free key must be classified:

`FREE_CREDENTIAL_REQUIRED`

not:

`PAID_PROVIDER_REQUIRED`.

# 8. FREE DATA DOES NOT AUTOMATICALLY MEAN FREE RIGHTS

For every customer-visible field and use, build or update a concise field-level matrix:

- product row;
- field ID;
- provider/source;
- source URL/document;
- terms/license version;
- checked date;
- exact semantic class;
- display right;
- commercial right;
- derived-data right;
- caching right;
- retention right;
- redistribution right;
- PDF/export right;
- attribution requirement;
- freshness/currentness contract;
- technical decision;
- legal-review state.

Use official terms, licenses and API documentation.

Possible technical states:

- `EXPLICITLY_PERMITTED_BY_OFFICIAL_TEXT`
- `EXPLICITLY_PROHIBITED`
- `AMBIGUOUS_LEGAL_REVIEW_REQUIRED`
- `FREE_CREDENTIAL_REQUIRED`
- `NOT_SUPPORTED`
- `WITHHELD`

Do not self-certify full legal compliance.

However, do not treat every rights question as an automatic blocker if explicit official text clearly grants the exact use. Record the proof and keep formal legal review as a later `GO_PAID`/legal gate where appropriate.

If a current provider is ambiguous or restrictive, actively search for a free/open/rights-safe alternative before recommending a paid provider.

# 9. AUTHORIZED NON-PRODUCTION INFRASTRUCTURE

Use existing authorized non-production capabilities.

You may:

- work locally;
- use owner-authorized local PostgreSQL/Supabase development;
- use an existing owner-authorized Supabase free-tier staging project;
- use non-production owner-controlled test accounts;
- use a non-production GitHub branch;
- use GitHub Actions included/free quota;
- use `windows-2025` GitHub-hosted runner if available and authorized;
- use free browser binaries and local testing tools;
- use public read-only providers according to their terms.

You may not:

- create a new external account or accept new Terms on behalf of the owner without approval;
- deploy production;
- merge to production;
- expose production secrets;
- send external outreach;
- run destructive production operations.

If a required credential is absent, create an exact minimal `OWNER_ACTION_REQUIRED` package, then continue all independent work.

# 10. STAGING MUST BE REAL, BUT MAY BE FREE

First detect:

- existing Supabase staging credentials;
- existing local Supabase CLI/Docker;
- existing PostgreSQL;
- existing GitHub/Supabase connections;
- current free project/quota availability.

Preferred proof order:

1. authorized free-tier Supabase staging;
2. authorized local Supabase stack;
3. authorized real local PostgreSQL for DB semantics, clearly labeled local;
4. mocks only for unit tests, never staging credit.

For authorized staging, execute:

- full ordered migrations;
- schema/migration parity;
- RLS;
- service-role separation;
- two real owner-controlled test JWTs;
- cross-account non-disclosure;
- atomicity;
- concurrency;
- rollback;
- idempotency;
- readback;
- immutable blob;
- same-blob preview/download/account paths;
- backup;
- controlled restore;
- permissions and hashes after restore.

No fake staging.

# 11. EXACT WINDOWS SERVER PROOF

P101R1 has exact Node/npm and strong Windows 11 proof, but not Windows Server 2025.

If authorized GitHub Actions and included/free minutes are available:

- create or use a dedicated non-production branch/workflow;
- use `windows-2025`;
- use exact Node `24.18.0`;
- use exact npm `11.16.0`;
- run current-byte TypeScript;
- run ESLint zero-warning;
- run Webpack;
- run Turbopack;
- run current runtime/smoke/browser gates that are appropriate;
- bind receipts to exact source SHA;
- do not expose production secrets.

Do not call Windows 11 “Windows Server 2025”.

If the free/included quota is unavailable, keep the exact Windows Server gate `WITHHELD` and continue.

# 12. UNIVERSAL CUSTOMER TABLE CONTRACT

Shield, Shield Pro and Real Markets tables must become actual reliable customer products, not decorative shells.

For every table, first enumerate the current canonical fields from code/catalog. Do not invent fields merely for appearance.

Every displayed field must have:

- canonical field ID;
- product/tier eligibility;
- semantic class;
- value;
- unit/precision;
- source/provider;
- source timestamp;
- received timestamp;
- freshness state;
- rights state;
- confidence/uncertainty if applicable;
- fallback state;
- customer-safe explanation;
- evidence reference where allowed.

Required UI states:

- `LOADING`
- `READY`
- `PARTIAL`
- `STALE`
- `WITHHELD`
- `UNAVAILABLE`
- `ERROR_CUSTOMER_SAFE`

A row may render `WITHHELD` without leaking its blocked value.

Never:

- expose an internal provider topology;
- show a blocked risk score;
- show a blocked confidence;
- label reference data as live/executable;
- convert a provider failure into zero;
- hide a stale timestamp;
- return a 500 where a controlled customer-safe state is required.

Test:

- desktop;
- mobile;
- keyboard;
- screen-reader semantics;
- sorting;
- filtering;
- search;
- pagination/virtualization where used;
- null values;
- stale values;
- duplicate rows;
- extreme decimals;
- long symbols/names;
- multiple currencies/chains;
- loading/error/empty states;
- PL/EN/DE.

# 13. SHIELD TABLES — B/P/A

Make the Shield family work end-to-end:

- `shield.basic`
- `shield.pro`
- `shield.advanced`

The table must use real rights-safe/current evidence when available.

Before any provider network call:

- execute deterministic field/provider rights preflight;
- confirm the provider is approved for that exact field and use;
- confirm currentness/freshness contract;
- confirm customer/tier eligibility.

When blocked:

- `riskScore = null`
- `confidence = null`
- `liveClaimed = false`
- `currentness = UNKNOWN_BLOCKED` or a more exact customer-safe state
- no provider calls;
- no internal evidence;
- no hidden result/publication/history/brain/kernel/source-sync/audit receipts.

When ready:

- render the actual customer table;
- prove row/field correctness;
- preserve canonical asset identity;
- bind current score to current evidence;
- integrate Risk History without mixing epochs;
- link to relevant Shield detail state.

Tier rules:

## Shield Basic

Must remain useful and show the approved core safety/risk information.

## Shield Pro tier

Must add material, matched-input value from the approved Shield contract, not merely more text.

## Shield Advanced

Must add material deeper evidence/resolution/context on the same canonical input.

Run matched-input comparison and prove no paid-data leak into Basic.

# 14. SHIELD PRO PRODUCT TABLES AND WORKFLOWS — B/P/A

Build and test the separate Shield Pro product as a real investigator/terminal/workflow product.

Required:

- real server-bound route;
- account/tenant authorization;
- case/workspace identity;
- table data;
- filters/search;
- state transitions;
- command/action boundaries;
- read-only vs mutating action separation;
- provider rights/currentness;
- entitlement isolation;
- concurrency;
- retry;
- recovery;
- error states;
- customer-safe logs/receipts;
- export where contractually required;
- PL/EN/DE;
- desktop/mobile/keyboard.

Do not leave Shield Pro Pro/Advanced as unreachable UI.

Use canonical IDs:

- `shield_pro.basic`
- `shield_pro.pro`
- `shield_pro.advanced`

For the same canonical case/input prove:

- Basic provides the approved core workflow;
- Pro adds material workflow/investigation capability;
- Advanced adds material adjudication/evidence/export capability;
- no silent downgrade;
- no entitlement bypass;
- no Pro/Advanced leakage into Basic.

If an existing product contract does not define a required tier delta, do not fabricate one. Record a precise `OWNER_DECISION_CANDIDATE`, while implementing all already-approved behavior.

# 15. REAL MARKETS TABLES — B/P/A

Make Real Markets tables actually work with honest semantics.

For every field classify exactly:

- `REFERENCE`
- `INDICATIVE`
- `DELAYED`
- `OFFICIAL_CLOSE`
- `CURRENT_QUOTE`
- `VENUE_QUOTE`
- `EXECUTABLE_QUOTE`
- `DERIVED`
- `HISTORICAL`

Do not promote:

- reference → live;
- indicative → executable;
- cached → current;
- daily official rate → market price.

For each field test:

- provider;
- rights;
- venue/session;
- timestamp/timezone;
- freshness;
- decimal precision;
- unit/currency;
- token decimals;
- conversion;
- rounding;
- null/missing values;
- zero/negative/overflow;
- stale cache;
- 250-row/bucket roundtrip;
- pagination;
- sorting/filtering;
- customer/tier eligibility.

Use real current free/right-safe data where available.

If no rights-safe current value exists, display a precise `WITHHELD/UNAVAILABLE` state rather than a fake fallback.

Tier rules:

- `real_markets.basic`: useful, honest core market/reference fields;
- `real_markets.pro`: material matched-input multi-source/deeper market intelligence;
- `real_markets.advanced`: material matched-input conflict/scenario/deeper intelligence.

Prove no Basic paid-data leakage and no silent tier downgrade.

# 16. BROWSER B/P/A

Promote Browser from strong local bounded proof to real internal customer FINAL.

Required:

- real owner-authorized input;
- rights-safe/current data;
- actual customer route;
- correct tier identity;
- durable store;
- render once;
- exact stored bytes;
- same-blob preview/download/account readback;
- entitlement;
- account isolation;
- fail-closed when durable storage unavailable;
- no implicit tier downgrade;
- PL/EN/DE;
- desktop/mobile;
- Chromium/Firefox/WebKit;
- Edge when available;
- keyboard/screen-reader/reduced motion;
- no hydration/input loss;
- no duplicate interactive DOM;
- safe CSP;
- runtime smoke;
- PDF artifact where required.

Matched input must prove Basic→Pro→Advanced material value.

# 17. AUDIT BASIC/PRO/ADVANCED — ACTUAL OPERATION

Make Audit work for the exact supported product contract.

First reconcile:

- V17/catalog promises;
- current UI claims;
- actual supported chains/input types;
- current BSC-only gate found in P101.

Do not overpromise URL/GitHub/all-EVM if the runtime cannot execute it.

If V17 requires those input types/chains, implement the missing secure execution path instead of hiding the gap.

If the authority permits a narrower launch scope, present the exact supported scope and fail closed before queueing unsupported inputs.

For supported inputs, execute:

input authorization  
→ exact chain/address/source identity  
→ safe evidence acquisition  
→ compiler/settings/deployment binding  
→ provider/evidence rights/currentness  
→ findings  
→ severity  
→ exploitability basis  
→ uncertainty  
→ remediation  
→ retest  
→ redaction  
→ tier output  
→ immutable PDF  
→ account delivery  
→ preview/download same bytes  
→ customer readback.

Maintain:

- Basic;
- Pro;
- Advanced;
- automated Advanced;
- no human reviewer release gate;
- no silent downgrade;
- lease validation before provider/PDF work;
- account-scoped request identity;
- no durable→memory split brain;
- caller-generated signed idempotency token;
- rollback on failure;
- no cross-account leakage.

# 18. AUDIT PROVIDER AND EVIDENCE MODEL

Preserve the separated dimensions:

- strict evidence receipts;
- successful live executions;
- independent provider families;
- customer-relevant evidence rows;
- field-level rights/currentness.

For Pro, use the current exact threshold contract unless a higher authority changes it.

For Advanced, use the current exact threshold contract.

Do not count:

- retries;
- mirrors;
- aliases;
- duplicate URLs;
- one upstream through multiple brands;
- partial/unverified evidence

as independent strict proof.

Search free/open/right-safe providers first.

Do not add a provider only to fill a number. Each source must add a real evidence class and independence.

# 19. AUDIT PDF — B/P/A

Generate professional customer-facing PDFs for Audit Basic, Pro and Advanced.

For each relevant tier/language:

- PL/EN/DE;
- exact tier identity;
- exact scope;
- findings;
- severity;
- evidence;
- uncertainty;
- remediation;
- retest;
- source/currentness;
- rights-safe content;
- no paid-data leakage;
- no internal topology;
- no raw Solidity/ABI/bytecode/trace/state leak;
- A4;
- embedded fonts;
- ToUnicode;
- text extraction;
- no active content;
- no unsafe annotations;
- no clipped identifiers;
- long digest wrapping;
- preview/download/account same blob;
- one SHA-256;
- exact byte length;
- immutable storage.

Use the exact external font only according to its proven license/binding. Do not package the binary if it is intentionally external.

# 20. INTERNAL CUSTOMER EXECUTION PACK

Create owner-controlled non-production customer executions for all rows where staging/local real runtime allows it.

Use:

- at least two owner-controlled customer accounts;
- distinct account IDs/JWTs;
- authorized inputs;
- real routes;
- real storage/readback;
- cross-account negatives;
- same output the customer sees;
- exact receipts.

This pack may support internal `Customer FINAL`.

Label it:

`OWNER_AUTHORIZED_INTERNAL_CUSTOMER_EXECUTION`

not:

`REAL_EXTERNAL_CUSTOMER`.

# 21. AI-SIMULATED CUSTOMER CAMPAIGN — START NOW

Execute a large, reproducible AI-simulated customer campaign before external pilot.

Unless a current V17 requirement is stricter, minimum campaign:

- **100 distinct AI customer personas**
- **24 journey steps per persona**
- at least **2400 journey-step executions**
- coverage across all 20 rows
- PL/EN/DE
- beginner to expert
- desktop/mobile
- success/partial/stale/withheld/error journeys.

Personas must include:

- beginner retail user;
- experienced crypto user;
- trader;
- analyst;
- token founder;
- protocol founder;
- Solidity developer;
- security engineer;
- institutional reviewer;
- compliance-oriented user;
- skeptical buyer;
- accessibility user;
- malicious/confused user;
- PL/EN/DE users.

Evaluate:

- can they understand the output;
- do they know what is current/stale/withheld;
- can they distinguish reference/live/executable;
- do they understand risk is `/100`, not probability;
- can they complete the task;
- do errors mislead;
- do tier boundaries make sense;
- do warnings remain visible;
- does the product leak internal data;
- are there contradictory product claims.

Do not self-credit this as external customer proof.

# 22. AI AUDITOR / REVIEWER PANEL

Run a full AI auditor/reviewer campaign on Audit Basic/Pro/Advanced.

Unless V17 is stricter, minimum:

- **50 contracts/cases**
- same canonical case through **3 tiers**
- at least **6 independent reviewer roles**
- at least **900 reviewer judgments**

Use:

- real historical vulnerable/fixed matched pairs;
- public disclosed incidents;
- hard negatives;
- lookalikes;
- mutations;
- metamorphic cases;
- development/validation/holdout separation.

Reviewer roles:

A. smart-contract security auditor  
B. exploitability/ground-truth reviewer  
C. false-positive/false-negative reviewer  
D. remediation/retest reviewer  
E. customer report/evidence reviewer  
F. rights/privacy/publication reviewer

For each case:

- blind the reviewers to unnecessary labels;
- preserve disagreement;
- require evidence;
- adjudicate conflicts;
- report TP/TN/FP/FN only for the exact corpus;
- never claim population-level 100% accuracy from a small corpus;
- compare matched input Basic/Pro/Advanced;
- assess whether paid tiers add material value;
- inspect final PDF/output;
- rerun affected cases after fixes.

# 23. ANGEL FULL EVALUATION — START NOW

Angel is one AI brain, not tiered.

Execute a baseline campaign now and a final campaign on frozen bytes.

Minimum unless V17 is stricter:

- at least **120 structured cases**;
- PL/EN/DE;
- repeated-run consistency;
- long context;
- cross-product questions;
- evidence citations/references;
- stale/conflicting/missing evidence;
- uncertainty;
- refusal/fail-closed;
- direct prompt injection;
- indirect prompt injection;
- malicious retrieved content;
- tool misuse;
- data exfiltration attempts;
- personal financial overconfidence;
- security overconfidence;
- unsupported claims;
- provider outage;
- fallback model behavior;
- memory deletion failure;
- privacy boundaries;
- age/provider/terms boundary;
- cost amplification/governor.

Do not send sensitive customer data to an external AI provider without explicit rights/privacy approval.

If Gemini or another external model is blocked, still execute:

- deterministic local/provider-mock safety tests;
- prompt/tool boundary tests;
- output schema tests;
- cost-governor tests;
- model-agnostic evaluation infrastructure.

Record the exact remaining provider/terms decision.

# 24. SECURITY ADVERSARIAL CAMPAIGN

Run defensive tests across the entire authorized Velmère surface:

- auth bypass;
- IDOR/BOLA;
- cross-account reads;
- RLS;
- service role;
- session replay;
- email verification;
- wallet nonce/domain/chain binding if used;
- SSRF;
- DNS rebinding;
- redirects;
- IP pinning;
- TLS hostname validation;
- request/response caps;
- rate limiting;
- cost amplification;
- XSS;
- CSRF;
- CORS;
- injection;
- file/archive traversal;
- PDF active content;
- cache poisoning;
- response swap;
- stale data;
- race conditions;
- concurrency;
- partial writes;
- orphan artifacts;
- replay;
- supply-chain poisoning;
- secret/log leakage;
- prompt injection;
- AI tool misuse;
- badge/Verify spoofing.

Use positive, negative, adversarial and independent verification where material.

No offensive testing of external systems.

# 25. WHALE WATCH

Make Whale Watch a full free product.

Execute with real read-only chain evidence where rights/currentness allow:

- chain ID;
- block number/hash;
- timestamp;
- finality;
- reorg behavior;
- token decimals;
- internal transfers;
- bridge transfers;
- spam/scam tokens;
- labels and label rights;
- attribution;
- double-count prevention;
- currentness;
- customer-safe evidence.

Never replace a missing provider timestamp with “now”.

# 26. MARKET IMPACT

Market Impact is free and must be fully functional.

Use real rights-safe order-book/depth data where available.

If not available:

- return `NO_USABLE_ORDER_BOOK`;
- `orderbook = null`;
- no execution scenario;
- `syntheticLiquidityUsed = false`;
- zero provider topology leak;
- no fake score.

Test:

- thin books;
- missing levels;
- stale books;
- cross-venue differences;
- malformed/spoof-like data;
- slippage;
- liquidity;
- calibration;
- uncertainty;
- provider failure.

# 27. SHIELD MAP

Make Shield Map work with:

- real node/edge source;
- exact identity;
- semantics;
- currentness;
- rights;
- confidence;
- privacy;
- deduplicated suggestions;
- canonical receipt ID;
- keyboard/mobile combobox;
- desktop/mobile;
- customer-safe errors.

Do not claim live/map truth without the required source.

# 28. RISK INDICATOR AND RISK HISTORY

Do not resume local-only Risk History polishing unless there is:

- deployed evidence;
- a material security/correctness defect;
- a cross-product truth defect.

Complete the real chain:

- staging migrations;
- service role;
- RLS;
- two JWTs;
- canonical/alias/private/ambiguous cases;
- pagination;
- concurrency/rollback;
- backup/restore;
- deployed HTTP;
- request/page binding;
- response-swap/cache negatives;
- customer input;
- immutable output.

Preserve approved UX:

- hover/focus → compact history;
- click/tap → full history;
- true temporal geometry;
- methodology segments;
- event markers;
- Basic useful;
- paid deeper explanations;
- no risk-as-probability confusion.

# 29. VELMÈRE VERIFY / CONTINUOUS VERIFICATION / BADGE

This is approved scope and must not be forgotten.

Implement and test production-quality:

- public Verify search;
- canonical chain + contract identity;
- optional project/token-name search;
- PUBLIC;
- PUBLIC_SUMMARY_PRIVATE_REPORT;
- PRIVATE;
- audit version/history;
- current status;
- last check;
- risk status;
- historical report visibility;
- material-change invalidation;
- VERIFIED;
- CHANGE_DETECTED;
- REVALIDATION_REQUIRED;
- REVALIDATING;
- VERIFIED_AGAIN;
- MONITORING_UNAVAILABLE;
- dynamic badge controlled by Velmère;
- badge links to canonical Verify page;
- no project-controlled green status;
- no stale CDN green status;
- no private-project enumeration;
- no arbitrary “sealed proof” for nonexistent IDs;
- no wrong-chain/address collision;
- no status replay.

These features have their own gates and do not add artificial customer rows.

# 30. CROSS-PRODUCT TRUTH

For the same canonical asset/project, ensure consistency across:

- Audit;
- Browser;
- Shield;
- Shield Pro;
- Real Markets;
- Shield Map;
- Market Impact;
- Whale Watch;
- Risk Indicator;
- Angel;
- Verify.

No two products may silently present contradictory current states.

Where timestamps differ, expose the difference.

Where methodologies differ, expose version/comparability boundaries.

Where evidence is unavailable, do not let Angel or another product invent certainty.

# 31. PAID TIER VALUE — 10/10

For each tiered family perform matched-input validation:

- Basic→Pro
- Pro→Advanced

Across:

- Audit;
- Browser;
- Shield;
- Shield Pro;
- Real Markets.

Exactly 10 transitions.

For each transition prove:

- same canonical input;
- same snapshot/time where appropriate;
- extra value comes from the tier;
- no artificial padding;
- no mere label change;
- no different easy/hard case;
- no Basic leak;
- no silent downgrade;
- entitlement enforced.

Record material customer benefit.

# 32. FUNCTIONAL UX BEFORE VISUAL POLISH

Before final visual polish, repair functional visual defects:

- overflow;
- clipping;
- duplicate interactive DOM;
- input loss before hydration;
- nested controls;
- inaccessible button;
- broken modal;
- focus loss;
- broken mobile;
- unusable table;
- misleading label;
- hidden warning;
- PDF layout defect.

Do not redesign for cosmetics until product/security freeze.

# 33. ACCESSIBILITY / BROWSER / DEVICE / LANGUAGE

Execute real started-server journeys on exact current builds.

At minimum:

- Chromium desktop/mobile;
- Firefox desktop/mobile;
- WebKit desktop/mobile;
- Edge when executable is available;
- keyboard;
- focus order;
- screen-reader semantics;
- reduced motion;
- zoom;
- touch targets;
- dialogs;
- table navigation;
- charts and alternatives;
- PL;
- EN;
- DE.

Do not call one browser proof cross-browser proof.

Preserve no-retry first failures and traces.

# 34. STABILITY / SOAK / FAILURE RECOVERY

Run all currently feasible stability testing:

- repeated startup/shutdown;
- production smoke;
- route loops;
- memory/RSS tracking;
- CPU;
- latency;
- burst;
- provider outage;
- DB outage;
- storage outage;
- queue retry/dead-letter;
- alert delivery;
- cache corruption;
- concurrent requests;
- repeated PDF generation;
- repeated Angel calls under cost governor;
- repeated table refresh;
- hydration;
- browser navigation.

Measure baselines; do not invent owner SLOs.

Where long soak cannot complete in the current session, create an authorized CI/local soak workflow and exact resumable receipt format.

# 35. OBSERVABILITY AND CUSTOMER-SAFE OPERATIONS

Validate:

- structured logs;
- metrics;
- traces/correlation;
- provider health;
- DB health;
- queue/backlog;
- alerts;
- errors;
- maintenance status;
- stale monitoring;
- revalidation failures;
- badge failures;
- Audit delivery failures.

No raw provider error, Supabase error, token, secret, private evidence, PII or internal topology in public responses/logs.

No false-green status on retry, dead-letter, missing sink or unfinished maintenance.

# 36. AUTH / ACCOUNT / PRIVACY

Complete all technically executable:

- signup;
- email verification;
- login/logout;
- refresh/expiry;
- session-family revocation;
- password reset;
- recovery;
- email change;
- account export;
- account deletion;
- Angel memory deletion;
- two-account isolation;
- account artifact ownership;
- admin/ops boundary;
- wallet auth if present.

Unconfirmed email must not receive a bound durable session/account state.

Durable delete failure must not return success.

# 37. BACKUP / RESTORE / RPO / RTO

If staging is authorized:

- create backup;
- perform controlled restore;
- compare row counts;
- compare event counts;
- compare digests;
- verify RLS;
- verify service-role grants;
- verify artifact hashes;
- verify same-blob delivery.

RPO/RTO remain an owner decision. Produce a short recommendation based on measured product behavior and free-tier capabilities.

# 38. SUPPLY CHAIN / CI

Close all possible:

- lockfile integrity;
- npm audit/advisory review;
- dependency signatures/inventory;
- SBOM;
- license inventory;
- current license binding;
- pinned GitHub Actions;
- minimal workflow permissions;
- secret isolation;
- artifact provenance;
- Windows path/reparse safety;
- deterministic packaging;
- no node_modules/cache/test PDFs/font binaries in SOURCE_ONLY.

Use hosted GitHub CI for exact Windows proof if authorized and free quota permits.

# 39. PERFORMANCE / COST SAFETY

Measure, do not invent:

- p50/p95/p99;
- error rate;
- CPU;
- memory/RSS;
- build size;
- DB latency;
- provider calls;
- PDF cost;
- AI request/token budget;
- candidate/model count;
- cache effectiveness;
- failover amplification.

Keep the cost governor before every AI planner/synthesis/shadow/text call.

Use bounded retries, circuit breakers and budgets.

# 40. AI, CUSTOMER AND REVIEWER RESULTS MUST DRIVE FIXES

Campaign output is not just a report.

For every material finding:

finding  
→ severity  
→ owner/product impact  
→ exact source/route  
→ fix  
→ positive test  
→ negative/adversarial test  
→ affected regression  
→ rerun affected campaign.

Do not simply record user confusion or reviewer disagreement and continue.

# 41. BENCHMARK LAB

After stable product outputs exist, run legal, defensive, reproducible benchmarks:

- public audit reports;
- historical vulnerable/fixed commits;
- disclosed incidents;
- public datasets;
- known ground truth.

Separate development/validation/holdout.

Do not cherry-pick.

Do not delete losing cases.

Do not claim 100% accuracy from a small corpus.

Do not perform offensive live exploitation.

# 42. EXTERNAL PILOT PREPARATION

Prepare, but do not fabricate:

- pilot eligibility checklist;
- authorization/consent template;
- project input checklist;
- private/public report option;
- disclosure/remediation flow;
- feedback survey;
- issue triage;
- pilot success metrics;
- rollback/support procedure;
- Verify/badge publication consent.

Do not contact anyone without owner authorization.

External pilot remains separate from internal FINAL and AI campaigns.

# 43. LEGAL/PRIVACY HANDOFF

Create an exact, concise lawyer-review pack:

- provider;
- exact fields;
- exact product/tier;
- display/derived/cache/retention/PDF/export use;
- official Terms/licence;
- version/date;
- technical interpretation;
- ambiguity;
- proposed decision.

Also include:

- privacy data map;
- subprocessors;
- Angel provider/model;
- user data;
- retention/deletion;
- international transfer;
- cookies/analytics;
- comparative marketing claims.

Do not replace legal counsel with model opinion.

# 44. NO LOCAL OVER-ENGINEERING

Once a local contract is sufficient to move to:

- staging;
- real provider;
- customer route;
- AI campaign;
- another product row;

move on.

Do not create v7/v8/v9 of the same local subsystem without new material evidence.

Do not keep polishing Risk History locally.

Do not build another meta-framework merely to count progress.

# 45. FINAL/DATA CREDIT RULES

`WITHHELD` does not mean broken UI.

A product can have a correct fail-closed UI and still be non-FINAL.

A row receives `FINAL` only when its own required chain is complete.

AI-simulated campaigns do not create external proof.

Owner-controlled staging execution may satisfy internal customer execution when every required technical and rights gate is met.

Real external pilot remains separate.

# 46. CURRENT 20-ROW BOARD MUST BE UPDATED

Start with all rows `WITHHELD` from P101R1.

For every row maintain:

- canonical ID;
- current state;
- real remaining blockers;
- free path available?;
- owner credential required?;
- legal review required?;
- staging required?;
- AI campaign status;
- matched-tier status;
- next physical action;
- reusable shared proof;
- final evidence reference.

Update after every material change.

# 47. PHASE ORDER

Use this order, but move to another executable workstream when blocked:

## Phase A — zero-euro environment activation

- verify P101R1;
- dependency/toolchain;
- authorized free staging;
- GitHub `windows-2025`;
- rights/free-provider matrix;
- exact two-account test setup.

## Phase B — customer product activation

- Shield B/P/A tables;
- Shield Pro B/P/A tables/workflows;
- Real Markets B/P/A tables;
- Audit B/P/A;
- Browser B/P/A;
- standalone products.

## Phase C — internal customer executions

- all 20 rows;
- owner-authorized accounts/inputs;
- storage/readback;
- exact outputs.

## Phase D — AI campaigns

Run baseline campaigns during Phase B/C and final campaigns after source freeze:

- 100 AI customers × 24 steps;
- 50 contracts × 3 tiers × ≥6 reviewers;
- ≥120 Angel cases;
- adversarial/security/reviewer panels.

## Phase E — global gates

- rights;
- auth;
- privacy;
- backup/restore;
- operations;
- supply chain;
- performance;
- accessibility;
- i18n;
- Verify;
- convergence.

## Phase F — visual polish and final regression

Only after product/security freeze.

# 48. DO NOT STOP ON AN EXTERNAL BLOCKER

If one row requires:

- a free key;
- staging credentials;
- legal review;
- owner decision;
- external participant;

record it and immediately continue another executable task.

A session may end only after:

- all authorized local engineering;
- all available staging work;
- all free-provider research/integration;
- all required AI-simulated customer/auditor/reviewer/adversarial campaigns;
- all available browser/accessibility/performance/security work;
- all material packaging

are exhausted.

# 49. UNIQUE CHECKPOINT IDENTITY

Follow Master V2 R1:

one checkpoint ID = one exact:

- source tree;
- ledger;
- SOURCE_ONLY;
- hash set;
- receipt set.

Before naming the next checkpoint, search all source/history/tags.

Do not reuse P100 or create another P101R1.

# 50. CHECKPOINTS ARE SAVEPOINTS

Create a checkpoint only after material:

- source repair;
- product execution;
- staging proof;
- rights decision;
- security closure;
- AI campaign;
- customer-row progress;
- meaningful global-gate progress.

Do not stop after checkpoint creation.

# 51. FINAL ENGINEERING GATES

For every material canonical candidate, on exact final bytes:

- targeted tests;
- negative/adversarial tests;
- affected regression;
- full semantic TypeScript;
- independent `tsc --noEmit` where useful;
- ESLint zero-warning;
- Webpack;
- Turbopack;
- production smoke;
- browser matrix;
- PDF corpus/visual inspection where affected;
- i18n;
- diff/manifest;
- exact Windows Server proof when available;
- source immutability.

Do not inherit gates from pre-fix bytes.

# 52. FINAL VISUAL POLISH

Only after functional/product/security freeze:

- coherent tables;
- hierarchy;
- spacing;
- typography;
- charts;
- risk-history popovers/dialogs;
- Verify pages;
- audit reports;
- loading/empty/error/withheld states;
- desktop/mobile;
- PL/EN/DE;
- accessibility.

Do not hide warnings or evidence for aesthetics.

# 53. POST-POLISH REGRESSION

After final polish rerun all affected:

- 20 customer rows;
- B/P/A tier isolation/value;
- rights/currentness;
- auth/RLS;
- PDFs;
- tables;
- Angel;
- Verify/badge;
- free modules;
- accessibility;
- browsers;
- builds;
- security;
- AI campaigns where UI/output changed.

# 54. CONVERGENCE

Run 3 clean convergence rounds on the final candidate as required by current authority.

A round is not clean if it finds a material defect.

After a material fix rerun the affected convergence scope.

Do not call AI simulated evidence human evidence.

# 55. PUBLIC CLAIMS

Remove or withhold any public number/claim without evidence binding.

Claims such as:

- coverage %;
- audited;
- verified;
- current;
- live;
- safe;
- low risk;
- best;
- world-class

must have exact meaning and evidence.

Do not publish competitor comparisons without exact version/scope/ground truth/methodology/legal review.

# 56. SAFETY BOUNDARY

Work only defensively on authorized Velmère and public read-only evidence.

Do not:

- exploit live external deployments;
- change external blockchain state;
- bypass external auth;
- steal credentials;
- create weaponized PoCs;
- deploy malware;
- create persistence;
- scan unauthorized systems;
- publish unremediated critical exploit details.

# 57. AUTONOMOUS LOOP

Repeat:

DISCOVER  
→ VERIFY  
→ RESEARCH  
→ MAP TO PRODUCT CONTRACT  
→ IMPLEMENT  
→ POSITIVE TEST  
→ NEGATIVE TEST  
→ ADVERSARIAL TEST  
→ INDEPENDENT VERIFY  
→ REGRESSION  
→ AI CUSTOMER/REVIEWER CHECK WHEN RELEVANT  
→ INTEGRATE  
→ CREDIT  
→ CHECKPOINT IF MATERIAL  
→ NEXT.

# 58. REQUIRED OUTPUTS

Maintain concise, real artifacts:

1. current 20-row closure board;
2. provider/field rights matrix;
3. free-provider candidate matrix;
4. staging execution receipts;
5. Windows Server receipts;
6. table field/tier contracts;
7. matched-input paid-value matrix;
8. internal customer execution registry;
9. AI customer campaign registry;
10. AI auditor/reviewer registry;
11. Angel evaluation registry;
12. security/adversarial findings;
13. accessibility/browser matrix;
14. performance/cost baseline;
15. external actions package;
16. current ledger;
17. deterministic SOURCE_ONLY.

Do not create bureaucracy without execution.

# 59. FINAL HANDOFF

At the end provide:

## A. Authority

- Master Directive V2 R1;
- V17;
- this owner override;
- latest checkpoint.

## B. Exact source

- SOURCE_ONLY;
- size;
- SHA-256;
- manifest;
- tree identity;
- Git commit/tree/tag if used.

## C. 20-row scoreboard

For every exact canonical row:

- FINAL/WITHHELD/OPEN;
- real executions;
- AI campaign result;
- rights state;
- staging state;
- remaining blocker.

## D. Tables/product status

Explicitly state:

- Shield B/P/A table state;
- Shield Pro B/P/A table/workflow state;
- Real Markets B/P/A table state;
- Audit B/P/A state;
- Browser B/P/A state;
- standalone product state.

## E. Test campaigns

- AI customers;
- AI auditors/reviewers;
- Angel;
- security/adversarial;
- browser/a11y/i18n;
- performance/stability.

## F. Global gates

- paid value;
- rights/currentness;
- security;
- privacy;
- auth;
- DB;
- backup/restore;
- operations;
- supply chain;
- accessibility;
- Verify;
- convergence.

## G. External actions

Only actions that truly cannot be performed autonomously, each with:

- reason;
- whether it can remain zero-cost;
- exact owner action;
- expected proof;
- whether it blocks internal FINAL, GO_PAID, LIVE or only external proof.

## H. No hidden worktree

Either promote material work to a canonical checkpoint or disclose exact unfinished diff/status.

# 60. SESSION END CONDITION

Do not stop merely because external pilot, legal review or production approval remains.

End only when one of these is true:

A. `FINAL_CANDIDATE` has been honestly achieved;

or

B. every remaining item is genuinely owner/external-only **and** all locally executable AI campaigns, product tests, provider research, free-path integration, browser/accessibility/security/performance work and packaging are complete;

or

C. the execution environment/limit physically ends the turn, in which case leave a fully resumable state.

# 61. OWNER-ACTION PACK FOR FREE STAGING

If no authorized staging credentials exist, do not just say “staging missing”.

Create a minimal exact package containing:

- free Supabase project requirements;
- required environment variable names;
- least-privilege keys;
- local/hosted choice;
- migrations to apply;
- test account A/B creation instructions;
- no production secrets;
- restore target requirement;
- exact command to resume;
- expected receipts.

Do not include or request secrets in chat output.

Then continue other work.

# 62. OWNER-ACTION PACK FOR FREE PROVIDER KEYS

For each missing free credential, produce:

- provider name;
- official free plan;
- exact required fields/limits;
- exact intended use;
- rights status;
- environment variable name;
- no raw key in logs;
- validation command;
- fallback if owner declines.

Do not create the account or accept terms without approval.

# 63. OWNER-ACTION PACK FOR RIGHTS REVIEW

Produce a lawyer-ready table, not a vague “legal review needed”.

Prioritize providers that can keep Velmère at zero cost.

For every ambiguous provider, also supply at least one free/open alternative if one exists.

# 64. OWNER-ACTION PACK FOR EXTERNAL PILOT

Prepare a zero-cost pilot workflow for small projects:

- authorization;
- scope;
- contract/chain;
- visibility choice;
- report publication consent;
- private findings handling;
- no active exploitation;
- feedback;
- Verify/badge;
- revalidation;
- case-study consent.

Do not send outreach without approval.

# 65. INTERNAL FINAL MUST NOT BE BLOCKED BY AI PANEL ORDER

AI campaigns may run before, during and after internal FINAL work.

Do not postpone them merely because the numerator is 0/20.

Do not promote a row solely because AI personas liked it.

Use campaigns to find and fix defects.

# 66. DATA/TABLE SUCCESS CRITERIA

A Shield/Shield Pro/Real Markets table is not “working” merely because it renders.

It is working only if:

- the correct product/tier route executes;
- row identity is correct;
- every field has known semantics;
- rights/currentness are enforced before network/display;
- real values are shown only when allowed;
- blocked values are absent;
- sorting/filtering/pagination/search behave correctly;
- mobile/keyboard/screen reader work;
- loading/partial/stale/error/withheld are truthful;
- no internal data leaks;
- no crash/500 on missing evidence;
- matched-tier value is proven;
- final started-server browser tests pass.

# 67. AUDIT SUCCESS CRITERIA

Audit is not “working” merely because a fixture PDF renders.

It is working only if:

- supported input scope is exact and truthful;
- authorized input reaches the worker;
- chain/source/deployment identity is exact;
- evidence is rights-safe/current;
- findings are grounded;
- severity/uncertainty/remediation/retest are correct;
- Basic/Pro/Advanced are materially distinct on matched input;
- Advanced remains automated;
- exact immutable PDF bytes are stored;
- account readback is authorized;
- preview/download/account bytes match;
- unsupported input fails before queue/provider work;
- no cross-account/lease/idempotency defect;
- real internal customer execution exists;
- AI reviewer panel passes the exact output;
- external pilot remains separately labeled.

# 68. ANGEL SUCCESS CRITERIA

Angel is not “working” merely because `/api/angel` returns text.

It is working only if:

- full policy/system prompt is applied;
- advice boundary is enforced;
- evidence/tool boundary is enforced;
- provider rights/privacy gate precedes transport;
- cost governor precedes all calls;
- memory lifecycle is fail-closed;
- unsupported/stale/conflicting evidence is handled;
- PL/EN/DE and long context pass;
- prompt-injection/tool-misuse campaigns pass;
- output is grounded and appropriately uncertain;
- external provider/age/privacy decisions are explicitly bound.

# 69. VERIFY SUCCESS CRITERIA

Verify is not “working” if it synthesizes proof from a URL or ID.

It is working only if a durable canonical publication registry exists and:

- nonexistent ID → 404/no-store;
- private project cannot be enumerated;
- current deployment identity is checked;
- history is versioned;
- material change invalidates current status;
- badge is dynamic and non-spoofable;
- old report remains historical, not current;
- monitoring failure yields MONITORING_UNAVAILABLE.

# 70. NO PAID DEPENDENCY BY DEFAULT

No product may be architecturally forced onto a paid provider merely because the previous registry used one.

First exhaust:

- direct chain evidence;
- explicit open license;
- official public data;
- free API;
- free key;
- free tier;
- owner-hosted computation.

If a paid provider is truly required for an approved product contract, present an exact owner decision with cost/value/lock-in and continue all unaffected work.

# 71. CURRENT OWNER GOAL

The owner expects:

- working Shield tables;
- working Shield Pro tables/workflows;
- working Real Markets tables;
- working Audit B/P/A;
- working Browser B/P/A;
- all free standalone modules tested;
- Angel deeply tested;
- site stability/security tested;
- AI customers/auditors/reviewers executed;
- zero-euro/free-first infrastructure and data wherever honestly possible;
- no fake live/current/rights claims;
- eventual 20/20 and a fully polished product.

Do not reduce this to another local safety framework.

Physically execute the product.

# 72. FINAL OWNER RULE

Do not optimize for a green ledger.

Do not optimize for pass count.

Do not stop because a legal review, pilot or one credential remains while other material work is executable.

Build the product, run it, test it as customers use it, attack its assumptions defensively, repair it, rerun it and package exact evidence.

When evidence is absent:

`WITHHELD`.

When data is stale:

`STALE`.

When there is no rights-safe source:

`UNAVAILABLE/WITHHELD`.

When a feature is not implemented:

`OPEN`.

When a product row has a complete authorized internal chain:

`FINAL`.

When real external users have not tested it:

`REAL_EXTERNAL_PROOF_REQUIRED`.

Velmère earns trust by never pretending certainty.

======================================================================

# START NOW

1. Verify sections 0–72 and the final sentinel.
2. Verify exact P101R1 authority, source, ledger and Git binding.
3. Build a lightweight current 20-row board.
4. Detect all available authorized free staging, free CI and free-provider paths.
5. Do not stop at “external actions”; create exact zero-cost owner-action packs and continue.
6. Make Shield B/P/A tables function end-to-end.
7. Make Shield Pro B/P/A tables/workflows function end-to-end.
8. Make Real Markets B/P/A tables function end-to-end.
9. Make Audit B/P/A and their PDFs function end-to-end.
10. Make Browser B/P/A function end-to-end.
11. Complete and test all standalone products.
12. Run the 100-persona / 2400-step AI customer campaign.
13. Run the 50-case × 3-tier × ≥6-reviewer Audit panel.
14. Run at least 120 Angel evaluation cases.
15. Run security, adversarial, stability, browser, accessibility, i18n and performance campaigns.
16. Use owner-controlled staging customers for internal customer execution where authorized.
17. Keep real external pilot separate.
18. Use free/open/public/free-tier providers first and verify rights per field/use.
19. Use GitHub `windows-2025` within authorized included/free quota when possible.
20. After each material savepoint, continue.
21. Pursue 20/20 honestly.
22. Continue through all technically executable global gates.
23. Perform final visual polish only after product/security freeze.
24. Rerun full post-polish regression and convergence.
25. Produce the exact final handoff.

**20/20 CUSTOMER FINAL.**

**FULL AI CUSTOMER / AUDITOR / REVIEWER CAMPAIGNS.**

**SHIELD, SHIELD PRO AND REAL MARKETS TABLES MUST ACTUALLY WORK.**

**AUDIT AND BROWSER BASIC/PRO/ADVANCED MUST ACTUALLY WORK.**

**FREE-FIRST / ZERO-EURO WHERE HONESTLY POSSIBLE.**

**ZERO FAKE CREDIT.**

**CHECKPOINT = SAVEPOINT.**

**CONTINUE UNTIL NO AUTHORIZED, EXECUTABLE, MATERIAL WORK REMAINS.**

======================================================================
END-OF-DIRECTIVE
EXPECTED LAST NUMBERED SECTION: 72
EXPECTED NUMBERED SECTIONS: 73 (0–72)
SECTIONS 0–72 MUST BE PRESENT BEFORE EXECUTION
======================================================================
