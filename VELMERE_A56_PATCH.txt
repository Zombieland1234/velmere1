PASS35 A56 — OUT-OF-TIME REPEATED SLO / ALERT-ACK / VENDOR-EXIT OBSERVATION ACCEPTANCE

- Adds read-only safe-ZIP intake for repeated A54 staging evidence instead of crediting one green drill.
- Requires at least three real A54 runs over at least 72 hours with at least 24 hours between runs.
- Requires one frozen A54 source identity, unique run/evidence/journal identities and successful restore in every run.
- Revalidates every required A54 check, strict metric floor, evidence manifest, duplicate receipt and journal lifecycle.
- Detects journal reordering, missing restore, digest mismatch, source drift, fixture evidence and failed recovery.
- Requires an Ed25519-signed independent real-time observer attestation for every run.
- Requires at least two independent observer organizations and two distinct observer keys.
- Requires stable primary identity and at least two distinct alternate provider/vendor/control-plane identities.
- Requires a dual-control aggregate attestation and an assurance chair independent from all run observers.
- Does not prove continuous monitoring, production SLO/SLA, production vendor exit, canary execution, LIVE or sale.
- No UI, asset, protected-engine, pricing, tier or product-data change.
