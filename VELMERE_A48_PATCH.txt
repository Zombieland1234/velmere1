VELMERE PASS35 A48 — STAGING TENANT ISOLATION ACCEPTANCE

Adds a fail-closed two-account staging acceptance harness. It requires verified A47 local acceptance,
exact Node 24.18.0/npm 11.16.0, a disposable non-production staging URL, two distinct Supabase test accounts
and a disposable staging database. It runs the existing RLS structural preflight, proves profile isolation,
requires disjoint private-artifact lists, rejects cross-tenant artifact reads and verifies session revocation.
No passwords, cookies or raw account identifiers are stored in evidence. Payments, email, storage, backups,
LIVE and sale readiness remain explicitly unproven.
