#!/usr/bin/env node
import fs from "node:fs"; import { createHash } from "node:crypto"; import { collectPass35Inventory } from "../pass35/source-inventory.mjs";
const REV="VELMERE_PASS36_A83R0_BROWSER_LENS_PDF_REAL_PACKET_MATRIX_AND_SECURE_DELIVERY_PARITY"; const read=p=>JSON.parse(fs.readFileSync(p,"utf8"));
const cj=v=>Array.isArray(v)?`[${v.map(cj).join(",")}]`:v&&typeof v==="object"?`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${cj(v[k])}`).join(",")}}`:JSON.stringify(v); const sha=v=>createHash("sha256").update(v).digest("hex");
const policy=read("config/pass36/a83-browser-lens-pdf-real-packet-policy.json"); const parent=read(policy.parentDescendantManifestPath); const pc={...parent}; delete pc.manifestDigestSha256; if(parent.manifestDigestSha256!==sha(cj(pc)))throw new Error("a83_parent_manifest_invalid");
const excluded=new Set(policy.descendantManifestExclusions); const inv=collectPass35Inventory(process.cwd()); if(inv.unknownCount!==0)throw new Error(`a83_unknown_inventory:${inv.unknownCount}`);
const rows=inv.entries.filter(r=>r.sourceIncluded&&!excluded.has(r.path)).map(r=>({path:r.path,byteLength:r.byteLength,sha256:r.sha256,mode:r.mode})).sort((a,b)=>a.path.localeCompare(b.path,"en"));
const payload={fileCount:rows.length,byteLength:rows.reduce((s,r)=>s+r.byteLength,0),pathSetSha256:sha(rows.map(r=>r.path).join("\n")),aggregateSha256:sha(rows.map(r=>`${r.path}\0${r.byteLength}\0${r.sha256}\0${r.mode}`).join("\n"))};
const core={schemaVersion:"velmere.pass36.a83.current-root-descendant-manifest.v1",revisionId:REV,parentRevisionId:policy.parentRevisionId,parentDescendantManifestDigestSha256:parent.manifestDigestSha256,generatedAt:policy.deterministicEpoch,payload,exclusions:[...excluded].sort(),claims:{browserLensPdfRealPacketMatrixImplemented:true,physicalPdfs:450,renderedPages:2100,realPacketsVerified:0,productionBrowserRuns:0,secureDeliveries:0,customerComprehensionLabels:0,paidGateEligible:false,liveProven:false,saleEnabled:false}};
const manifest={...core,manifestDigestSha256:sha(cj(core))}; fs.writeFileSync(policy.descendantManifestPath,`${JSON.stringify(manifest,null,2)}\n`); console.log(JSON.stringify({status:"PASS_A83_DESCENDANT_MANIFEST_BUILD",output:policy.descendantManifestPath,payload,manifestDigestSha256:manifest.manifestDigestSha256},null,2));
