#!/usr/bin/env node
import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import {REV,PARENT,MANIFEST,PARENT_MANIFEST,RECEIPT,sha256,canonicalJson,readJson,collect,payload} from "./a102r38-source-boundary.mjs";const root=process.cwd(),m=readJson(root,MANIFEST),p=readJson(root,PARENT_MANIFEST);let checks=0;const ok=(v,id)=>{checks++;assert.ok(v,id)};ok(m.revisionId===REV&&m.parentRevisionId===PARENT,"identity");ok(p.revisionId===PARENT&&m.parentDescendantManifestDigestSha256===p.manifestDigestSha256,"parent");const inv=collect(root);ok(inv.rejected.length===0,"rejected");ok(JSON.stringify(m.payload)===JSON.stringify(payload(inv.rows)),"payload");ok(m.localRegressionReceiptSha256===sha256(fs.readFileSync(path.join(root,RECEIPT))),"receipt");const c={...m};delete c.manifestDigestSha256;ok(m.manifestDigestSha256===sha256(canonicalJson(c)),"digest");ok(m.claims.a102r38EvidenceChecks===46&&m.claims.productionSmokeContractChecks===82,"target");ok(m.claims.expectedSmokeAssertions===55&&m.claims.uniqueSmokeAssertions===55&&m.claims.expectedSmokeResults===16&&m.claims.uniqueSmokeResults===16,"denominator");ok(m.claims.sourceAuthorityReconciled&&m.claims.historicalR37R4UniqueAssertions===52&&!m.claims.historicalR37R4Smoke55Of55Credit,"truth");ok(m.claims.formalRemainingEntries===31&&!m.claims.exactBuildBrowserCredit,"formal");ok(!m.claims.liveProven&&!m.claims.saleEnabled&&!m.claims.productionApproved&&!m.claims.worldClassProven,"promotion");console.log(JSON.stringify({status:"PASS_A102R38_DESCENDANT_ACTION_REQUIRED_NO_PROMOTION",checksPassed:checks,checksFailed:0,payload:m.payload,manifestDigestSha256:m.manifestDigestSha256},null,2));
