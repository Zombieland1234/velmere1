#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../..");const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),"utf8"));const rev="VELMERE_PASS36_A102R44P26_ACTION_REQUIRED_TRUSTED_EXTERNAL_EVIDENCE_SEMANTIC_STAGING_DENOMINATORS_AND_EXACT_RUNTIME_BRIDGE_NO_LIVE_CREDIT";const parent="VELMERE_PASS36_A102R44P25_ACTION_REQUIRED_AUTHORITY_FORK_RECONCILIATION_CURRENT_RELEASE_POINTER_STAGING_ADMISSION_WINDOWS_HANDOFF_AND_SIGNED_EVIDENCE_NO_LIVE_CREDIT";const a=read("config/pass36/current-release-authority.json");const c=read("config/current-release.json");const p=read("config/pass35/current-revision.json");const checks=[];const add=(id,ok)=>checks.push({id,ok:Boolean(ok)});add("authority-revision",a.authorityRevisionId===rev);add("authority-parent",a.parentRevisionId===parent);add("source-revision",a.sourceRevisionId===rev&&a.sourceParentRevisionId===parent);add("current-source",a.currentSource?.revisionId===rev&&a.currentSource?.parentRevisionId===parent);add("roadmap-plane",a.planes?.roadmapProgram?.revisionId===rev&&a.planes?.roadmapProgram?.path==="config/pass36/a102r44p26-world-class-completion-program.json");add("r44p26-plane",a.planes?.a102r44p26TrustedExternalEvidenceSemanticClosure?.semanticDenominator===62&&a.planes?.a102r44p26TrustedExternalEvidenceSemanticClosure?.externalStagingCredit===false);add("claims-current",a.claims?.currentRevisionId===rev&&a.claims?.parentRevisionId===parent);add("claims-no-go",a.claims?.decision==="NO_GO"&&a.claims?.saleEnabled===false&&a.claims?.productionApproved===false&&a.claims?.worldClassProven===false);add("current-release",c.sourceRevisionId===rev&&c.sourceParentRevisionId===parent&&c.productionPromotionAllowed===false);add("current-status",c.status==="ACTION_REQUIRED_NO_PROMOTION");add("current-flags",c.saleEnabled===false&&c.productionApproved===false&&c.worldClassProven===false);add("pass35",p.sourceRevisionId===rev&&p.currentRevisionId===rev&&p.parentRevisionId===parent);add("active",fs.readFileSync(path.join(root,"VELMERE_ACTIVE_PASS.txt"),"utf8").trim()===rev);add("program",fs.existsSync(path.join(root,"config/pass36/a102r44p26-world-class-completion-program.json")));add("legacy-not-current",a.authorityRevisionId!==parent);const failed=checks.filter(r=>!r.ok);console.log(JSON.stringify({schemaVersion:"velmere.pass36.a102r44p26.current-release-pointer-verification.v1",status:failed.length?"FAIL":"PASS_R44P26_CURRENT_RELEASE_POINTERS",checks:checks.length,passed:checks.length-failed.length,failed:failed.length,rows:checks},null,2));if(failed.length)process.exit(1);
