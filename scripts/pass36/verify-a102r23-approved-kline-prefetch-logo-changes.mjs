#!/usr/bin/env node
import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const REV='VELMERE_PASS36_A102R23_ACTION_REQUIRED_KLINE_MODE_LOCAL_REFERENCE_DETAIL_ICON_REQUEST_AND_DEV_PREFETCH_STORM_RECOVERY_NO_REAL_CREDIT';const PARENT='VELMERE_PASS36_A102R22_ACTION_REQUIRED_LOCAL_DEV_RUNTIME_DATA_REFERENCE_ICON_AND_CONTINUOUS_ROUTE_TRANSITION_RECOVERY_NO_REAL_CREDIT';const ledger=JSON.parse(fs.readFileSync(path.join(root,'config/pass36/a102r23-approved-kline-prefetch-logo-changes.json'),'utf8'));const sha=b=>crypto.createHash('sha256').update(b).digest('hex');let checks=0;const ok=(v,id)=>{checks++;assert.ok(v,id)};
ok(ledger.revisionId===REV,'revision');ok(ledger.parentRevisionId===PARENT,'parent');ok(ledger.fileCount===8,'file_count');ok(Array.isArray(ledger.approvedFiles)&&ledger.approvedFiles.length===8,'rows');ok(new Set(ledger.approvedFiles.map(r=>r.path)).size===8,'unique');
for(const row of ledger.approvedFiles){const bytes=fs.readFileSync(path.join(root,row.path));ok(bytes.length===row.currentByteLength,`bytes:${row.path}`);ok(sha(bytes)===row.currentSha256,`sha:${row.path}`);ok(row.parentSha256!==row.currentSha256,`changed:${row.path}`);}
console.log(JSON.stringify({status:'PASS_A102R23_APPROVED_KLINE_PREFETCH_LOGO_CHANGES_NO_PROMOTION',checksPassed:checks,checksFailed:0,fileCount:8},null,2));
