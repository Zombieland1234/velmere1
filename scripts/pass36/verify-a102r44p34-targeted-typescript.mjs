#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";import { pathToFileURL } from "node:url";const tsRoot=process.env.VELMERE_EXACT_TYPESCRIPT_ROOT; if(!tsRoot)throw new Error("VELMERE_EXACT_TYPESCRIPT_ROOT_REQUIRED");const ts=await import(pathToFileURL(path.join(tsRoot,"lib/typescript.js")));if(ts.version!=="5.9.3")throw new Error(`TYPESCRIPT_VERSION_MISMATCH:${ts.version}`);const files=["lib/product/vlm-canonical-product-topology.ts","lib/commerce/vlm-current-sku-truth.ts","lib/ai/angel-route-policy.ts"];const rows=[];for(const file of files){const source=fs.readFileSync(file,"utf8");const out=ts.transpileModule(source,{fileName:file,compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext,strict:true,isolatedModules:true,verbatimModuleSyntax:true},reportDiagnostics:true});const diagnostics=(out.diagnostics??[]).filter(d=>d.category===ts.DiagnosticCategory.Error).map(d=>ts.flattenDiagnosticMessageText(d.messageText,"\n"));rows.push({file,passed:diagnostics.length===0,diagnostics});}const failed=rows.filter(r=>!r.passed);console.log(JSON.stringify({schemaVersion:"velmere.pass36.a102r44p34.targeted-typescript.v1",status:failed.length?"FAIL":"PASS_R44P34_TARGETED_TYPESCRIPT",typescriptVersion:ts.version,files:rows.length,passed:rows.length-failed.length,failed:failed.length,rows},null,2));if(failed.length)process.exit(1);
