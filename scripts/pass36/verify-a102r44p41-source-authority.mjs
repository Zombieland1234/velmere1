#!/usr/bin/env node
import crypto from "node:crypto";import fs from "node:fs";import path from "node:path";
const REV="VELMERE_PASS36_A102R44P41_ACTION_REQUIRED_OFFICIAL_FOUNDRY_INVARIANTS_ANVIL_RAW_RPC_EXACT_OFFLINE_FULL_LINUX_RELEASE_NO_LIVE_CREDIT";
const root=path.resolve(process.argv[2]??process.cwd());const relativeManifest="_velmere/PASS36_A102R44P41_SOURCE_ONLY_MANIFEST.json";const manifestPath=path.join(root,...relativeManifest.split("/"));
const fail=(message,extra={})=>{console.log(JSON.stringify({status:"FAIL_R44P41_SOURCE_AUTHORITY",message,...extra},null,2));process.exit(1);};
const excludedParts=new Set(["node_modules",".git",".next",".turbo",".cache","coverage","test-results","playwright-report","__pycache__",".pytest_cache","tmp","temp","out","cache","failures"]);
const excluded=(relativePath)=>relativePath.split("/").some((part)=>excludedParts.has(part)||part.startsWith(".next-"));const compareUtf8=(a,b)=>Buffer.compare(Buffer.from(a,"utf8"),Buffer.from(b,"utf8"));const sha256=(bytes)=>crypto.createHash("sha256").update(bytes).digest("hex");
function walk(directory,base=""){const rows=[];for(const entry of fs.readdirSync(directory,{withFileTypes:true})){const relative=base?`${base}/${entry.name}`:entry.name;if(excluded(relative))continue;const absolute=path.join(directory,entry.name);const stat=fs.lstatSync(absolute);if(stat.isSymbolicLink())fail("SYMLINK_FORBIDDEN",{path:relative});if(entry.isDirectory())rows.push(...walk(absolute,relative));else if(entry.isFile()&&relative!==relativeManifest){const bytes=fs.readFileSync(absolute);rows.push({path:relative,byteLength:bytes.length,sha256:sha256(bytes)});}}return rows;}
if(!fs.existsSync(manifestPath))fail("MANIFEST_MISSING");const manifest=JSON.parse(fs.readFileSync(manifestPath,"utf8"));if(manifest.revisionId!==REV)fail("REVISION_MISMATCH",{actual:manifest.revisionId});if(manifest.pathOrderAlgorithm!=="UTF8_BYTEWISE_ASCENDING_V1")fail("PATH_ORDER_ALGORITHM_MISMATCH");const actual=walk(root).sort((a,b)=>compareUtf8(a.path,b.path));const expected=[...manifest.files].sort((a,b)=>compareUtf8(a.path,b.path));if(actual.length!==expected.length)fail("FILE_COUNT_MISMATCH",{actual:actual.length,expected:expected.length});for(let i=0;i<actual.length;i++){if(actual[i].path!==expected[i].path||actual[i].byteLength!==expected[i].byteLength||actual[i].sha256!==expected[i].sha256)fail("FILE_BINDING_MISMATCH",{index:i,actual:actual[i],expected:expected[i]});}
const aggregate=crypto.createHash("sha256");for(const row of actual)aggregate.update(`${row.path}\0${row.byteLength}\0${row.sha256}\n`);const sourceAggregateSha256=aggregate.digest("hex");const pathSetSha256=sha256(Buffer.from(`${actual.map(r=>r.path).join("\n")}\n`,"utf8"));const payloadBytes=actual.reduce((s,r)=>s+r.byteLength,0);if(sourceAggregateSha256!==manifest.sourceAggregateSha256)fail("AGGREGATE_MISMATCH",{actual:sourceAggregateSha256,expected:manifest.sourceAggregateSha256});if(pathSetSha256!==manifest.pathSetSha256)fail("PATH_SET_MISMATCH");if(manifest.fileCount!==actual.length||manifest.payloadBytes!==payloadBytes)fail("TOTAL_MISMATCH");console.log(JSON.stringify({status:"PASS_R44P41_SOURCE_AUTHORITY",revisionId:REV,fileCount:actual.length,payloadBytes,sourceAggregateSha256,pathSetSha256,sourceImmutable:true},null,2));
