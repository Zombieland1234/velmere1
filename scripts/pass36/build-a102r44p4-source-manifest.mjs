#!/usr/bin/env node
import crypto from "node:crypto";import fs from "node:fs";import path from "node:path";
const REVISION="VELMERE_PASS36_A102R44P4_ACTION_REQUIRED_OFFICIAL_TOOLCHAIN_200_EXECUTIONS_SEMGREP_MIGRATION_AND_AUTOMATED_AUDIT_EVIDENCE_BINDING_NO_LIVE_CREDIT";const PARENT="VELMERE_PASS36_A102R44P3_ACTION_REQUIRED_CURRENT_BYTE_SHIELD_PRO_REAL_MARKETS_AND_MULTILINGUAL_AI_MATRIX_CLOSURE_NO_LIVE_CREDIT";const MANIFEST_REL="_velmere/PASS36_A102R44P4_SOURCE_MANIFEST.json";const PARENT_MANIFEST_REL="_velmere/PASS36_A102R44P3_SOURCE_MANIFEST.json";
const EXCLUDED_TOP=new Set(["artifacts",".velmere","node_modules",".cache",".turbo","coverage","test-results","playwright-report","__pycache__"]);const root=process.cwd();const sha=b=>crypto.createHash("sha256").update(b).digest("hex");
function collect(dir,prefix=""){const rows=[];for(const e of fs.readdirSync(dir,{withFileTypes:true}).sort((a,b)=>Buffer.compare(Buffer.from(a.name),Buffer.from(b.name)))){if(!prefix&&(EXCLUDED_TOP.has(e.name)||e.name.startsWith(".next")))continue;const rel=prefix?`${prefix}/${e.name}`:e.name;if(rel===MANIFEST_REL)continue;const abs=path.join(dir,e.name);if(e.isSymbolicLink())throw new Error(`symlink_forbidden:${rel}`);if(e.isDirectory())rows.push(...collect(abs,rel));else if(e.isFile()){const b=fs.readFileSync(abs);rows.push({path:rel.split(path.sep).join("/"),byteLength:b.length,sha256:sha(b),mode:fs.statSync(abs).mode});}}return rows;}
const rows=collect(root).sort((a,b)=>Buffer.compare(Buffer.from(a.path),Buffer.from(b.path)));const pathSetSha256=sha(Buffer.from(`${rows.map(r=>r.path).join("\n")}\n`));const ag=crypto.createHash("sha256");for(const r of rows)ag.update(`${r.path}\0${r.byteLength}\0${r.sha256}\0${r.mode}\n`);const aggregateSha256=ag.digest("hex");
const pp=path.join(root,PARENT_MANIFEST_REL),pb=fs.readFileSync(pp),pm=JSON.parse(pb);if(pm.revisionId!==PARENT)throw new Error("parent_revision_mismatch");const parent=new Map(pm.entries.map(r=>[r.path,r]));parent.set(PARENT_MANIFEST_REL,{path:PARENT_MANIFEST_REL,byteLength:pb.length,sha256:sha(pb),mode:fs.statSync(pp).mode});const current=new Map(rows.map(r=>[r.path,r]));const changes=[];for(const p of [...new Set([...parent.keys(),...current.keys()])].sort((a,b)=>Buffer.compare(Buffer.from(a),Buffer.from(b)))){const a=parent.get(p),b=current.get(p);if(!a&&b)changes.push({path:p,changeType:"ADDED",parent:null,current:b});else if(a&&!b)changes.push({path:p,changeType:"DELETED",parent:a,current:null});else if(a&&b&&(a.byteLength!==b.byteLength||a.sha256!==b.sha256||a.mode!==b.mode))changes.push({path:p,changeType:"MODIFIED",parent:a,current:b});}
const manifest={schemaVersion:"velmere.pass36.a102r44p4.source-manifest.v1",revisionId:REVISION,parentRevisionId:PARENT,generatedAt:"2026-08-02T09:25:00.000Z",parentSourceManifestPath:PARENT_MANIFEST_REL,parentSourceManifestSha256:sha(pb),parentSourceAggregateSha256:pm.aggregateSha256,manifestExcludedPath:MANIFEST_REL,exclusions:[...EXCLUDED_TOP,".next*"],fileCount:rows.length,byteLength:rows.reduce((s,r)=>s+r.byteLength,0),pathSetSha256,aggregateSha256,changeCounts:{added:changes.filter(x=>x.changeType==="ADDED").length,modified:changes.filter(x=>x.changeType==="MODIFIED").length,deleted:changes.filter(x=>x.changeType==="DELETED").length,total:changes.length},changes,globalDecision:"NO_GO",live:false,saleEnabled:false,productionApproved:false,worldClassProven:false,entries:rows,truthBoundary:"Complete clean R44P4 source identity. Official tool process evidence is local/CI only and grants no real audit, customer, Windows, staging, LIVE or sale credit."};fs.mkdirSync(path.dirname(path.join(root,MANIFEST_REL)),{recursive:true});fs.writeFileSync(path.join(root,MANIFEST_REL),JSON.stringify(manifest,null,2)+"\n");console.log(JSON.stringify({status:"PASS_A102R44P4_SOURCE_MANIFEST_BUILT",revisionId:REVISION,manifestPath:MANIFEST_REL,manifestSha256:sha(fs.readFileSync(path.join(root,MANIFEST_REL))),fileCount:manifest.fileCount,byteLength:manifest.byteLength,pathSetSha256,aggregateSha256,changeCounts:manifest.changeCounts,globalDecision:"NO_GO",live:false,saleEnabled:false},null,2));
