#!/usr/bin/env node
import assert from "node:assert/strict";
import { existsSync, readFileSync } from "node:fs";
import { runA29Benchmark, verifyA29Benchmark, verifyA29Policy } from "../../lib/security/pass35-a29-upgrade-deployment-operations-runtime.mjs";
const read = (p) => JSON.parse(readFileSync(p, "utf8"));
let assertions = 0;
const check = (value, message) => { assert.ok(value, message); assertions += 1; };
const policy = read("config/pass35/a29-upgrade-deployment-operations-policy.json");
const contract = read("config/pass35/a29-upgrade-deployment-operations-runtime-contract.json");
const receipt = read("artifacts/pass35/PASS35_A29_UPGRADE_DEPLOYMENT_OPERATIONS_RECEIPT.json");
const status = read("config/pass35/current-status-register.json");
const zero = read("config/pass35/zero-budget-functional-roadmap.json");
const current = read("config/current-release.json");
const audit = read("config/pass35/audit-program.json");
const envelope = read("config/pass35/audit-execution-envelope.json");
const product = read("config/pass35/product-tier-content-contract.json");
check(verifyA29Policy(policy), "policy");
const runtime = runA29Benchmark(policy);
check(verifyA29Benchmark(runtime, policy), "runtime");
check(runtime.denominators.cases === 192 && runtime.denominators.frozen === 72 && runtime.denominators.mutations === 2304 && runtime.denominators.families === 12, "denominators");
check(runtime.frozen.accuracy === 1 && runtime.frozen.unsafeEligible === 0 && runtime.frozen.falseBlocks === 0 && runtime.mutation.killRate === 1, "metrics");
check(contract.canonicalWeightedPlanningPercent === 55.8 && contract.canonicalStrictDonePercent === 32.6 && contract.zeroBudgetWeightedPlanningPercent === 90.9, "percentages");
check(contract.progressDeltaVsA28.canonicalPercentagePoints === 1.1 && contract.progressDeltaVsA28.strictPercentagePoints === 2.4 && contract.progressDeltaVsA28.zeroBudgetPercentagePoints === 0.4, "delta A28");
check(contract.progressDeltaVsA16.canonicalPercentagePoints === 15.1 && contract.progressDeltaVsA16.strictPercentagePoints === 23.3 && contract.progressDeltaVsA16.zeroBudgetPercentagePoints === 10.9, "delta A16");
check(receipt.targetProxySlotBindingComplete && receipt.upgradeAuthorizationComplete && receipt.multisigQuorumAndTimelockComplete && receipt.initializerAndStorageLayoutGateComplete && receipt.upgradeSimulationAndRollbackGateComplete && receipt.pauseEmergencyControlsComplete && receipt.keyRotationRecoveryAndCompromiseScenariosComplete && receipt.deterministicOperationsReplayComplete && receipt.mutationScoreGateComplete, "receipt implementation");
check(!receipt.currentOnChainStateVerified && !receipt.realMultisigTimelockExecuted && !receipt.productionUpgradeExecuted && !receipt.qualifiedHumanReviewed && !receipt.independentRerun && !receipt.paidGateEligible && !receipt.sellEnabled, "receipt truth");
const row = status.rows.find((item) => item.id === "AUD13_A11_UPGRADE_DEPLOYMENT_OPS");
check(row?.status === "DONE" && row.blocker === "NONE_LOCAL_FOR_DECLARED_BOUNDED_A11_UPGRADE_DEPLOYMENT_OPERATIONS_EVIDENCE_IMPLEMENTATION" && row.missing.length === 0, "status row");
for (const id of ["ZB77_A11_PROXY_AUTH_MULTISIG_TIMELOCK_BINDINGS", "ZB78_A11_STORAGE_ROLLBACK_EMERGENCY_KEY_CONTROLS", "ZB79_A11_OPERATIONS_REPLAY_FROZEN_BENCHMARK"]) check(zero.capabilities.find((item) => item.id === id)?.status === "DONE", `zero ${id}`);
check(zero.capabilities.filter((item) => !new Set(zero.zeroBudgetCoreExclusions).has(item.id)).length === 86, "zero denominator");
check(audit.controls.find((item) => item.id === "A11")?.status === "IMPLEMENTED_LOCAL_UPGRADE_DEPLOYMENT_OPERATIONS_EVIDENCE_BENCHMARKED_ONCHAIN_DRILL_REVIEW_MISSING", "audit status");
const family = envelope.capabilityInventory.find((item) => item.familyId === "upgrade_deployment_operations");
check(family?.state === "IMPLEMENTED_LOCAL_UPGRADE_DEPLOYMENT_OPERATIONS_EVIDENCE_BENCHMARKED_ONCHAIN_DRILL_REVIEW_MISSING" && family.mayNotClaim.includes("A11 paid gate passed"), "envelope");
const surface = product.surfaces.find((item) => item.surfaceId === "audit_evm");
for (const field of product.a29UpgradeDeploymentOperations.requiredProFields) check(surface.tiers.pro.requiredFields.includes(field), `pro ${field}`);
for (const field of product.a29UpgradeDeploymentOperations.requiredAdvancedFields) check(surface.tiers.advanced.requiredFields.includes(field), `advanced ${field}`);
check(current.sourceRevisionId === policy.sourceRevisionId && current.a29UpgradeDeploymentOperationsContractPath === "config/pass35/a29-upgrade-deployment-operations-runtime-contract.json", "current pointer");
check(JSON.stringify(status.statusPrecedence) === JSON.stringify(["this register", "A32 roadmap current-status section", "machine-generated readiness dashboard", "A31 and earlier addenda as historical implementation notes only", "base roadmap as target requirements"]), "precedence");
for (const file of ["artifacts/release/PASS35_A29_UPGRADE_DEPLOYMENT_OPERATIONS.md", "artifacts/release/PASS35_A29_PRODUCT_ROADMAP_SUMMARY.json", "VELMERE_WORLD_CLASS_MAX_ROADMAP_PASS35.txt"]) check(existsSync(file), `missing ${file}`);
const roadmap = readFileSync("VELMERE_WORLD_CLASS_MAX_ROADMAP_PASS35.txt", "utf8");
check(roadmap.startsWith("====================================================================================================\nPASS35 A32 — REPORT DELIVERY EVIDENCE"), "roadmap header");
check(roadmap.includes("HISTORYCZNE PODSUMOWANIE A31 I WCZEŚNIEJSZYCH FAL") && !roadmap.includes("PASS35 A29 is the only canonical current status."), "roadmap history");
console.log(JSON.stringify({ status: "PASS_A29_CONTROL_PLANE", assertions, cases: runtime.denominators.cases, mutations: runtime.denominators.mutations, canonical: contract.canonicalWeightedPlanningPercent, strict: contract.canonicalStrictDonePercent, zeroBudget: contract.zeroBudgetWeightedPlanningPercent, sellEnabled: 0 }, null, 2));
