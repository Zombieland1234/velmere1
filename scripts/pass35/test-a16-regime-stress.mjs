import assert from'node:assert/strict';import{runPass35A16CrossAssetPortfolioRuntime}from'../../lib/market-integrity/pass35-a16-cross-asset-portfolio-runtime.mjs';import{runPass35A16RegimeStressRuntime,verifyPass35A16RegimeStressRuntime}from'../../lib/market-integrity/pass35-a16-regime-stress-runtime.mjs';import{buildA16PortfolioFixtures,buildA16MarketWindows}from'./a16-test-fixtures.mjs';let c=0;const x=(v,m)=>{c++;assert.ok(v,m)},p=runPass35A16CrossAssetPortfolioRuntime(buildA16PortfolioFixtures()),r=runPass35A16RegimeStressRuntime({marketWindows:buildA16MarketWindows(),portfolioRows:p.rows});x(verifyPass35A16RegimeStressRuntime(r),'verify');x(r.scenarioResultCount===72,'count');for(const q of['CALM','RISK_ON','RISK_OFF','STRESS','DISLOCATION'])x(r.regimes.some(a=>a.regime===q),q);const t=structuredClone(r);t.scenarioResultCount=1;x(!verifyPass35A16RegimeStressRuntime(t),'tamper');console.log(JSON.stringify({status:'PASS_A16_REGIME_STRESS',checks:c,windows:5,scenarios:6,scenarioResults:72},null,2));
