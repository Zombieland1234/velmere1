import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{buildPass35A16CanonicalChannelParityRuntime,verifyPass35A16CanonicalChannelParityRuntime}from'../../lib/market-integrity/pass35-a16-canonical-channel-parity.ts';let c=0;const x=(v:unknown,m:string)=>{c++;assert.ok(v,m)},p=JSON.parse(readFileSync('config/pass35/product-tier-content-contract.json','utf8')),r=buildPass35A16CanonicalChannelParityRuntime({productContract:p});x(verifyPass35A16CanonicalChannelParityRuntime(r),'verify');x(r.tierPacketCount===21&&r.projectionCount===126,'counts');x(r.packets.every((a)=>a.projections.length===6),'channels');x(r.packets.filter((a)=>a.tier==='basic').every((a)=>a.projections.find((b)=>b.channel==='pdf')?.pageContract===2),'basic pages');x(r.packets.filter((a)=>a.tier==='pro').every((a)=>a.projections.find((b)=>b.channel==='pdf')?.pageContract===4),'pro pages');x(r.packets.filter((a)=>a.tier==='advanced').every((a)=>a.projections.find((b)=>b.channel==='pdf')?.pageContract===8),'adv pages');const t=structuredClone(r);t.packets[0].projections[0].packetHash='0'.repeat(64);x(!verifyPass35A16CanonicalChannelParityRuntime(t),'tamper');console.log(JSON.stringify({status:'PASS_A16_CANONICAL_CHANNEL_PARITY',checks:c,tierPackets:21,projections:126,visualChangesMade:false,sellEnabled:false},null,2));
