#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";import process from "node:process";import {spawnSync} from "node:child_process";import {currentNpmVersion} from "./lib/velmere-runtime-contract.mjs";
const root=process.cwd();const write=process.argv.includes("--write");const staticOnly=process.argv.includes("--static");const contract=JSON.parse(fs.readFileSync(path.join(root,"config/pass35/a57-controlled-canary-kill-switch-rollback-telemetry-acceptance.json"),"utf8"));const checks=[];const check=(id,ok,detail=null,skipped=false)=>checks.push({id,ok:Boolean(ok),detail,skipped:Boolean(skipped)});
const required=["config/pass35/a57-controlled-canary-kill-switch-rollback-telemetry-acceptance.json","config/pass35/a57-source-manifest.json","scripts/a57-controlled-canary-kill-switch-rollback-telemetry-acceptance.mjs","scripts/lib/a57-canary-validation.mjs","scripts/a57-package-evidence.mjs","scripts/a57-runtime-diagnostics.mjs","scripts/pass35/test-a57-controlled-canary-kill-switch-rollback-telemetry-acceptance.mjs","scripts/pass35/test-a57-controlled-canary-kill-switch-rollback-telemetry-fixture.mjs","scripts/pass35/verify-a57-source-manifest.mjs","VELMERE_RUN_A57_CONTROLLED_CANARY_KILL_SWITCH_ROLLBACK_TELEMETRY_ACCEPTANCE.cmd","VELMERE_A57_PATCH.txt"];
for(const file of required)check(`file:${file}`,fs.existsSync(path.join(root,file)),file);check("contract:revision",contract.revisionId==="VELMERE_PASS35_A57_CONTROLLED_CANARY_KILL_SWITCH_ROLLBACK_TELEMETRY_ACCEPTANCE",contract.revisionId);check("contract:bounds",contract.canary?.maximumParticipants===25&&contract.canary?.maximumTestTransactions===10&&contract.canary?.minimumSnapshots===5,contract.canary);check("contract:stop-rules",contract.stopRules?.availabilityMinimumPct===99&&contract.stopRules?.securityIncidentsMaximum===0&&contract.stopRules?.failedRefundsMaximum===0,contract.stopRules);
const regression=spawnSync(process.execPath,["scripts/pass35/test-a57-controlled-canary-kill-switch-rollback-telemetry-acceptance.mjs"],{cwd:root,encoding:"utf8",maxBuffer:128*1024*1024});check("regression:a57_contract",regression.status===0,{status:regression.status,stdout:regression.stdout?.trim(),stderr:regression.stderr?.trim()});
if(staticOnly){check("runtime:node_exact",true,{observed:process.versions.node,expected:contract.runtime.node},true);check("runtime:npm_exact",true,{observed:currentNpmVersion(),expected:contract.runtime.npm},true);}else{check("runtime:node_exact",process.versions.node===contract.runtime.node,{observed:process.versions.node,expected:contract.runtime.node});check("runtime:npm_exact",currentNpmVersion()===contract.runtime.npm,{observed:currentNpmVersion(),expected:contract.runtime.npm});}
for(const name of contract.requiredEnvironment)check(`env:${name}`,staticOnly||Boolean(process.env[name]),{present:Boolean(process.env[name])},staticOnly);const failures=checks.filter((row)=>!row.ok);const report={schemaVersion:"velmere.pass35.a57.runtime-diagnostics.v1",revisionId:contract.revisionId,generatedAt:new Date().toISOString(),truthBoundary:staticOnly?"Static A57 diagnostics; no real controlled canary, customer outcome, LIVE, production or sale credit.":"Exact A57 preflight; no secret values are persisted.",runtime:{node:process.versions.node,npm:currentNpmVersion(),expectedNode:contract.runtime.node,expectedNpm:contract.runtime.npm},summary:{checks:checks.length,passed:checks.length-failures.length,failed:failures.length,skipped:checks.filter((row)=>row.skipped).length},failures,checks};if(write){const target=path.join(root,"artifacts/pass35/a57/PASS35_A57_RUNTIME_DIAGNOSTICS.json");fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,`${JSON.stringify(report,null,2)}\n`);}console.log(JSON.stringify(report.summary,null,2));if(failures.length)process.exit(1);
