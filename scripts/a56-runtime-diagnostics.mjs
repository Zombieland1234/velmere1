#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";import process from "node:process";import {spawnSync} from "node:child_process";import {currentNpmVersion} from "./lib/velmere-runtime-contract.mjs";
const root=process.cwd();const write=process.argv.includes("--write");const staticOnly=process.argv.includes("--static");const contract=JSON.parse(fs.readFileSync(path.join(root,"config/pass35/a56-out-of-time-slo-vendor-exit-observation-acceptance.json"),"utf8"));const checks=[];const check=(id,ok,detail=null,skipped=false)=>checks.push({id,ok:Boolean(ok),detail,skipped:Boolean(skipped)});
const required=["config/pass35/a56-out-of-time-slo-vendor-exit-observation-acceptance.json","config/pass35/a56-source-manifest.json","scripts/a56-out-of-time-slo-vendor-exit-observation-acceptance.mjs","scripts/a56-package-evidence.mjs","scripts/a56-runtime-diagnostics.mjs","scripts/pass35/test-a56-out-of-time-slo-vendor-exit-observation-acceptance.mjs","scripts/pass35/test-a56-out-of-time-slo-vendor-exit-observation-fixture.mjs","scripts/pass35/verify-a56-source-manifest.mjs","VELMERE_RUN_A56_OUT_OF_TIME_SLO_VENDOR_EXIT_OBSERVATION_ACCEPTANCE.cmd","VELMERE_A56_PATCH.txt"];
for(const file of required)check(`file:${file}`,fs.existsSync(path.join(root,file)),file);check("contract:revision",contract.revisionId==="VELMERE_PASS35_A56_OUT_OF_TIME_SLO_VENDOR_EXIT_OBSERVATION_ACCEPTANCE",contract.revisionId);check("contract:runs",contract.observation?.minimumRuns===3&&contract.observation?.minimumObservationSpanSeconds===259200&&contract.observation?.minimumGapBetweenRunsSeconds===86400,contract.observation);check("contract:env",contract.requiredEnvironment?.length===8,contract.requiredEnvironment);
const regression=spawnSync(process.execPath,["scripts/pass35/test-a56-out-of-time-slo-vendor-exit-observation-acceptance.mjs"],{cwd:root,encoding:"utf8",maxBuffer:128*1024*1024});check("regression:a56_contract",regression.status===0,{status:regression.status,stdout:regression.stdout?.trim(),stderr:regression.stderr?.trim()});
if(staticOnly){check("runtime:node_exact",true,{observed:process.versions.node,expected:contract.runtime.node},true);check("runtime:npm_exact",true,{observed:currentNpmVersion(),expected:contract.runtime.npm},true);}else{check("runtime:node_exact",process.versions.node===contract.runtime.node,{observed:process.versions.node,expected:contract.runtime.node});check("runtime:npm_exact",currentNpmVersion()===contract.runtime.npm,{observed:currentNpmVersion(),expected:contract.runtime.npm});}
for(const name of contract.requiredEnvironment)check(`env:${name}`,staticOnly||Boolean(process.env[name]),{present:Boolean(process.env[name])},staticOnly);const failures=checks.filter((r)=>!r.ok);const report={schemaVersion:"velmere.pass35.a56.runtime-diagnostics.v1",revisionId:contract.revisionId,generatedAt:new Date().toISOString(),truthBoundary:staticOnly?"Static A56 diagnostics; no out-of-time staging, canary, continuous monitoring, LIVE or sale credit.":"Exact A56 preflight; paths and hashes only, no private key material.",runtime:{node:process.versions.node,npm:currentNpmVersion(),expectedNode:contract.runtime.node,expectedNpm:contract.runtime.npm},summary:{checks:checks.length,passed:checks.length-failures.length,failed:failures.length,skipped:checks.filter((r)=>r.skipped).length},failures,checks};if(write){const target=path.join(root,"artifacts/pass35/a56/PASS35_A56_RUNTIME_DIAGNOSTICS.json");fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,`${JSON.stringify(report,null,2)}\n`);}console.log(JSON.stringify(report.summary,null,2));if(failures.length)process.exit(1);
