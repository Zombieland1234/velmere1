/**
 * R10 legacy quarantine.
 *
 * This historical generator synthesized solver/proof metadata, fixed detector totals,
 * and execution statistics without exact-scope receipts. It is intentionally disabled
 * so it cannot regenerate customer artifacts with unsupported evidence claims.
 *
 * Use the R10 evidence-bound report pipeline and canonical corpus migration instead.
 */

throw new Error(
  "R10_LEGACY_GENERATOR_DISABLED: generate_180_dowody8.ts is historical and cannot issue release artifacts without exact-scope evidence receipts.",
);
